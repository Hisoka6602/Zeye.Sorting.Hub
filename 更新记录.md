# 更新记录（CHANGELOG）

> 本文件记录每次 PR 更新的详细内容，按时间倒序排列（最新在前）。

## 本次更新内容（长期数据库底座 PR-P：报表查询隔离与只读副本预留）

### 背景
在 PR-O 已补齐备份、恢复、校验与演练底座后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-P。执行前先核对现有台账，确认当前长期数据库底座已完成到 PR-O，因此本次集中补齐报表查询隔离、只读副本预留与预算守卫基础能力。

### 新增内容

1. **报表查询只读副本底座**
   - 新增 `ReadOnlyDatabaseOptions`、`ReadOnlyDbContextFactorySelector`。
   - 统一定义只读副本开关、主库回退策略、报表时间范围预算与最大返回行数上限，并支持按当前 Provider 解析 `ConnectionStrings:MySqlReadOnly` / `ConnectionStrings:SqlServerReadOnly`。

2. **报表查询预算守卫**
   - 新增 `ReportingQueryGuard` 与 `ReportingQueryBudget`。
   - 统一校验本地时间语义、限制最大时间范围、裁剪最大返回行数，并强制关闭总数统计，避免后续报表查询拖垮在线链路。

3. **宿主接线与健康探针**
   - 新增 `ReadOnlyDatabaseHealthCheck`。
   - 调整 `Program.cs` 与 `PersistenceServiceCollectionExtensions.cs`，将只读副本路由能力接入依赖注入与 `/health/ready` 健康检查链路。

4. **配置与测试同步**
   - 在 `appsettings.json` 中新增 `Persistence:ReadOnlyDatabase` 配置节，并补齐只读副本连接字符串预留键。
   - 新增 `ReportingQueryIsolationTests.cs`，覆盖时间范围预算、行数裁剪、只读副本缺失时的主库回退与直接拒绝策略。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座P-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/ReadModels/ReadOnlyDatabaseOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/ReadModels/ReadOnlyDbContextFactorySelector.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/ReadModels/ReportingQueryGuard.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/ReadModels/ReportingQueryBudget.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/ReadOnlyDatabaseHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/ReportingQueryIsolationTests.cs`
- 新增：`检查台账/PR-长期数据库底座P-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-P 断点摘要

### 已完成
- 只读数据库配置底座
- 报表查询预算
- 报表查询隔离守卫

### 保留能力
- 当前已支持按 MySQL / SQL Server 配置独立只读副本连接字符串，并在只读副本缺失或不可用时按配置执行主库回退或直接拒绝。
- 报表查询预算已统一约束本地时间范围、最大返回行数，并默认关闭总数统计。
- `/health/ready` 已补充只读副本状态探针，可直接暴露当前路由目标与退化状态。

### 未完成但已预留
- PR-Q 租户 / 站点 / 设备维度数据边界预留
- 后续可在当前底座上接入真实报表查询服务，但必须复用 `ReportingQueryGuard` 与 `ReadOnlyDbContextFactorySelector`

### 下一 PR 入口
- 下一 PR 从 PR-Q“租户 / 站点 / 设备维度数据边界预留”开始
- 后续不要重复实现报表查询时间范围校验、只读副本回退与总数统计关闭逻辑，应复用 `ReportingQueryGuard` 与 `ReadOnlyDbContextFactorySelector`

## 本次更新内容（长期数据库底座 PR-O：备份、恢复、校验与演练底座）

### 背景
在 PR-N 已补齐数据保留策略、自动清理治理与健康检查后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-O。执行前先核对现有台账，确认当前长期数据库底座已完成到 PR-N，因此本次集中补齐备份计划、Provider 备份命令生成、备份文件校验与恢复演练底座。

### 新增内容

1. **备份治理底座**
   - 新增 `BackupOptions`、`BackupPlan`、`BackupExecutionRecord`、`IBackupProvider`、`MySqlBackupProvider`、`SqlServerBackupProvider`、`BackupCommandTextFormatter`。
   - 统一定义备份目录、Runbook 目录、演练目录、最新备份年龄阈值，以及 MySQL/SQL Server 的备份命令生成、数据库名校验与命令转义语义。

2. **恢复资产与运行期校验**
   - 新增 `RestoreDrillPlanner`，统一输出数据库恢复 Runbook 与演练记录资产。
   - 新增 `BackupVerificationService`，串联“生成计划 → 查找最新备份 → 判断是否超龄 → 缓存最近执行记录”。
   - 修正禁用场景短路顺序：`IsEnabled=false` 时不再强依赖连接字符串，可直接返回 Disabled/Healthy。
   - 将 Runbook / 演练记录写入改为“临时文件 + 原子替换”，避免异常时丢失历史资产。

3. **宿主接线与健康探针**
   - 新增 `BackupHostedService`，按轮询周期执行备份治理校验。
   - 新增 `BackupHealthCheck`，对外暴露最新备份文件、Runbook、演练记录与备份时效状态。
   - 调整 `Program.cs` 与 `PersistenceServiceCollectionExtensions.cs`，将备份治理能力接入宿主启动流程与基础设施注册。

4. **配置与测试同步**
   - 在 `appsettings.json` 中新增 `Persistence:Backup` 配置节，补齐中文注释、允许范围与默认目录示例。
   - 调整 `.gitignore`，为 `Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/` 源码目录解除默认 Backup* 忽略规则，避免底座源码被误排除。
   - 新增 `BackupGovernanceTests.cs`，覆盖 Provider 命令生成安全性、禁用场景无连接串、最新备份校验、Runbook/演练记录输出与健康检查状态。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座O-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupPlan.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupExecutionRecord.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/IBackupProvider.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/MySqlBackupProvider.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/SqlServerBackupProvider.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupFileNamePolicy.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupConnectionStringValueReader.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupCommandTextFormatter.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/RestoreDrillPlanner.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Backup/BackupVerificationService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/BackupHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/BackupHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/BackupGovernanceTests.cs`
- 新增：`检查台账/PR-长期数据库底座O-检查台账.md`
- 修改：`.gitignore`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-O 断点摘要

### 已完成
- 备份计划
- Provider 备份命令生成
- 备份校验
- 恢复演练记录

### 保留能力
- 当前已按 MySQL / SQL Server 分别生成备份命令，不在运行期自动执行真实备份或真实恢复
- 最近一次备份治理结果会缓存在进程内，供健康检查直接读取最新备份文件、Runbook 与演练记录状态
- 恢复 Runbook 与演练记录默认按配置目录落盘，生产恢复仍保持“仅生成指导文档，不自动执行危险恢复”的边界

### 未完成但已预留
- PR-P 报表查询隔离与只读副本预留
- 后续可在当前底座上继续补充真实备份执行器、多副本校验与归档上传，但不得突破人工恢复门禁

### 下一 PR 入口
- 下一 PR 从 PR-P“报表查询隔离与只读副本预留”开始
- 后续不要重复实现数据库名解析、Provider 命令拼装、备份文件命名与恢复文档输出，应复用 `IBackupProvider`、`BackupVerificationService` 与 `RestoreDrillPlanner`

## 本次更新内容（长期数据库底座 PR-N：数据保留策略与自动清理治理）

### 背景
在 PR-M 已补齐 Inbox 幂等消费、失败重试与过期治理候选后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-N。执行前先核对现有台账，确认当前长期数据库底座已完成到 PR-M，因此本次集中补齐数据保留策略、自动清理治理与运行期健康检查基础能力。

### 新增内容

1. **数据保留治理底座**
   - 新增 `DataRetentionOptions`、`DataRetentionPolicy`、`DataRetentionPlanner`、`DataRetentionExecutor`、`DataRetentionAuditRecord`。
   - 统一定义保留对象名称、默认保留天数、批次大小、危险动作守卫、dry-run 与最近一次治理审计模型。

2. **运行期接线与健康探针**
   - 新增 `DataRetentionHostedService`，按固定轮询周期执行计划统计、危险动作决策与逐策略清理。
   - 新增 `DataRetentionHealthCheck`，对外暴露最近一次治理状态、决策、计划量、执行量与失败策略数。
   - 调整 `Program.cs` 与 `PersistenceServiceCollectionExtensions.cs`，将数据保留治理能力接入宿主启动流程与基础设施注册。

3. **受治理对象覆盖扩展**
   - `DataRetentionPlanner` 已覆盖 `WebRequestAuditLog`、`OutboxMessage`、`InboxMessage`、`IdempotencyRecord`、`ArchiveTask`、`DeadLetterWriteEntry` 与 `SlowQueryProfile` 候选统计/清理入口。
   - 扩展 `DeadLetterWriteStore` 与 `SlowQueryProfileStore`，集中提供受限批次统计与清理能力，避免治理链路内复制同义逻辑。

4. **配置与测试同步**
   - 在 `appsettings.json` 中新增 `Persistence:Retention` 配置节，补齐中文注释、允许范围与默认策略示例。
   - 新增 `DataRetentionTests.cs`，覆盖 dry-run、真实清理与健康检查禁用路径。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座N-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Retention/DataRetentionOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Retention/DataRetentionPolicy.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Retention/DataRetentionPlanner.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Retention/DataRetentionExecutor.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Retention/DataRetentionAuditRecord.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/DataRetentionHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/DataRetentionHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/DataRetentionTests.cs`
- 新增：`检查台账/PR-长期数据库底座N-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryProfileStore.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/DeadLetterWriteStore.cs`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-N 断点摘要

### 已完成
- 数据保留策略
- 清理计划
- dry-run 执行器
- 保留治理健康检查

### 保留能力
- 当前已统一支持数据库内实体与内存态死信/慢查询画像的候选统计与受限批次治理入口
- 危险动作守卫优先于 dry-run，真实清理必须显式放开 `AllowDangerousActionExecution`
- 最近一次治理结果会以审计记录形式缓存在进程内，供健康检查与后续治理观测复用

### 未完成但已预留
- PR-O 备份、恢复、校验与演练底座
- 后续可将数据保留审计记录继续沉淀为可查询的持久化资产或只读接口

### 下一 PR 入口
- 下一 PR 从 PR-O“备份、恢复、校验与演练底座”开始
- 后续不要重复实现保留策略解析、危险动作决策与分策略清理循环，应复用 `DataRetentionExecutor` 与 `DataRetentionPlanner`

## 本次更新内容（长期数据库底座 PR-M：Inbox 幂等消费底座）

### 背景
在 PR-L 已补齐 Outbox 事件持久化、状态推进与健康探测后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-M。执行前先核对现有台账，确认当前长期数据库底座已完成到 PR-L，因此本次集中补齐 Inbox 幂等消费、失败重试与过期治理基础能力。

### 新增内容

1. **Inbox 领域模型与仓储契约**
   - 新增 `InboxMessage`、`InboxMessageStatus`、`IInboxMessageRepository`。
   - 统一定义 `SourceSystem + MessageId` 唯一消息键、消费状态、失败消息、重试次数与过期治理时间。

2. **Inbox 守卫与基础设施落地**
   - 新增 `InboxMessageGuardService`，统一协调首次消费、处理中拒绝、成功回放与失败重试。
   - 新增 `InboxMessageRepository`、`InboxMessageEntityTypeConfiguration`，通过唯一索引约束消息键组合，并提供过期治理候选查询能力。

3. **Host 运行期接线**
   - 调整 `Program.cs`，注册 `InboxMessageGuardService`。
   - 调整 `PersistenceServiceCollectionExtensions.cs`，接入 `IInboxMessageRepository` 实现，供后续真实消费链路复用。

4. **迁移与测试**
   - 新增 EF 迁移 `20260507021744_AddInboxMessageSupport.*` 与模型快照更新。
   - 新增 `InboxMessageTests.cs`，覆盖首次消费、重复回放、处理中拒绝、失败重试与过期治理候选。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座M-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Domain/Aggregates/Events/InboxMessage.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Enums/Events/InboxMessageStatus.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/IInboxMessageRepository.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Events/InboxMessageGuardService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/EntityConfigurations/InboxMessageEntityTypeConfiguration.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Repositories/InboxMessageRepository.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/InboxMessageTests.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/20260507021744_AddInboxMessageSupport.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/20260507021744_AddInboxMessageSupport.Designer.cs`
- 新增：`检查台账/PR-长期数据库底座M-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Domain/Repositories/Models/Results/RepositoryErrorCodes.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/SortingHubDbContextModelSnapshot.cs`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-M 断点摘要

### 已完成
- InboxMessage 聚合
- Inbox 幂等消费记录
- 消费状态治理
- 过期治理候选查询

### 保留能力
- 当前已提供 `SourceSystem + MessageId` 唯一消息键保护
- 成功消费记录支持回放已有结果，处理中消息明确拒绝，失败消息可在重试上限内重新接管
- 当前已保留 `ExpiresAt` 本地时间治理边界，后续清理能力可直接复用

### 未完成但已预留
- PR-N 数据保留策略与自动清理治理
- 后续将 Inbox 守卫接入真实外部事件消费链路

### 下一 PR 入口
- 下一 PR 从 PR-N“数据保留策略与自动清理治理”开始
- 后续不要重复实现消息键唯一校验、消费状态流转与过期候选查询，应复用 `InboxMessageGuardService` 与 `IInboxMessageRepository`

## 本次更新内容（长期数据库底座 PR-L：Outbox 事件底座与业务事件持久化）

### 背景
在 PR-K 已补齐写入幂等、重复请求回放与重复键治理后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-L。执行前先核对现有台账，确认当前长期数据库底座已完成到 PR-K，因此本次集中补齐 Outbox 事件持久化、状态推进、失败隔离与健康探测基础能力。

### 新增内容

1. **Outbox 领域模型与仓储契约**
   - 新增 `OutboxMessage`、`OutboxMessageStatus`、`IOutboxMessageRepository`。
   - 新增 `OutboxMessageHealthSnapshotReadModel`，统一定义待处理/处理中/失败/死信计数与最早积压时间读模型。

2. **Outbox 应用服务与外部合同**
   - 新增 `AppendOutboxMessageCommandService`、`DispatchOutboxMessageCommandService`、`GetOutboxMessagePagedQueryService`、`OutboxMessageContractMapper`。
   - 新增 `OutboxMessageCreateRequest`、`OutboxMessageListRequest`、`OutboxMessageListResponse`、`OutboxMessageResponse`。

3. **Host 运行期接线**
   - 调整 `DataGovernanceApiRouteExtensions`，新增 `/api/data-governance/outbox-messages` 写入与分页查询入口。
   - 调整 `Program.cs`，接入 `OutboxDispatchHostedService`、`OutboxHealthCheck` 与 Outbox 应用服务注册。

4. **基础设施落地**
   - 新增 `OutboxMessageRepository` 与 `OutboxMessageEntityTypeConfiguration`。
   - 新增 EF 迁移 `20260506175929_AddOutboxMessageSupport.*`，补齐 `OutboxMessages` 表及状态/事件类型索引。

5. **测试与文档同步**
   - 新增 `OutboxMessageTests.cs`，覆盖写入、分页、状态推进、死信与健康检查。
   - 新增 `检查台账/PR-长期数据库底座L-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Domain/Aggregates/Events/OutboxMessage.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Enums/Events/OutboxMessageStatus.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/IOutboxMessageRepository.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/Models/ReadModels/OutboxMessageHealthSnapshotReadModel.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Events/AppendOutboxMessageCommandService.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Events/DispatchOutboxMessageCommandService.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Events/GetOutboxMessagePagedQueryService.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Events/OutboxMessageContractMapper.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Events/OutboxMessageCreateRequest.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Events/OutboxMessageListRequest.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Events/OutboxMessageListResponse.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Events/OutboxMessageResponse.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/EntityConfigurations/OutboxMessageEntityTypeConfiguration.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Repositories/OutboxMessageRepository.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/OutboxDispatchHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/OutboxHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/OutboxMessageTests.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/20260506175929_AddOutboxMessageSupport.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/20260506175929_AddOutboxMessageSupport.Designer.cs`
- 新增：`检查台账/PR-长期数据库底座L-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Host/Routing/DataGovernanceApiRouteExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/SortingHubDbContextModelSnapshot.cs`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-L 断点摘要

### 已完成
- OutboxMessage 聚合
- Outbox 仓储
- Outbox 状态流转
- Outbox 健康检查

### 保留能力
- 当前已提供独立写入入口与后台日志派发模拟，不依赖外部 MQ
- 可派发消息通过仓储原子领取，避免同一消息被重复推进到 Processing
- 非法 JSON 载荷会在有限重试后进入死信，避免无限重试

### 未完成但已预留
- PR-M Inbox 幂等消费底座
- 后续将 Outbox 写入并入真实业务事务的同 DbContext 协同链路

### 下一 PR 入口
- 下一 PR 从 PR-M“Inbox 幂等消费底座”开始
- 后续不要重复实现 Outbox 状态推进与健康快照查询，应复用 `DispatchOutboxMessageCommandService`、`GetOutboxMessagePagedQueryService` 与 `IOutboxMessageRepository`

## 本次更新内容（长期数据库底座 PR-K：写入幂等、去重与重复键治理）

### 背景
在 PR-J 已补齐查询模板治理、模板覆盖报告与只读索引建议闭环后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-K。执行前先核对现有台账，确认当前长期数据库底座已完成到 PR-J，因此本次集中补齐写入幂等、SHA256 载荷哈希、重复请求回放与重复键治理底座。

### 新增内容

1. **幂等领域模型与仓储契约**
   - 新增 `IdempotencyRecord`、`IdempotencyRecordStatus`、`IIdempotencyRepository`。
   - 统一定义 `SourceSystem + OperationName + BusinessKey + PayloadHash` 幂等键模型与状态流转。

2. **幂等守卫与基础设施落地**
   - 新增 `IdempotencyGuardService`、`IdempotencyGuardException`、`IdempotencyRepository`、`IdempotencyKeyHasher`。
   - 新增 `IdempotencyRecordEntityTypeConfiguration`，通过唯一索引约束幂等键组合。
   - 新增 `DuplicateKeyExceptionDetector`，集中复用唯一键冲突识别逻辑。

3. **管理端新增写路径接入**
   - 调整 `CreateParcelCommandService` 与 `ParcelAdminApiRouteExtensions`。
   - 管理端新增包裹入口改为基于规范化本地时间载荷计算 SHA256 哈希，相同请求重复提交时回放已有结果，处理中请求返回明确冲突。
   - 审查修正后，调用方基于稳定错误码判定幂等处理中，不再依赖异常消息文本；取消请求会持久化为可重试的 `Rejected` 状态，Pending 且已有真实结果时可自动回放恢复，避免长期卡死。

4. **迁移与测试**
   - 新增 EF 迁移 `20260506075656_AddIdempotencyRecordSupport.*` 与模型快照更新。
   - 新增 `IdempotencyTests.cs`，覆盖哈希稳定性、重复请求回放与 Pending 状态拒绝。
   - 修正 `ParcelAdminApiTests`、`ParcelBufferedWriteTests` 的测试应用依赖注册。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座K-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Domain/Aggregates/Idempotency/IdempotencyRecord.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Enums/Idempotency/IdempotencyRecordStatus.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/IIdempotencyRepository.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Idempotency/IdempotencyGuardService.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Idempotency/IdempotencyGuardException.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Idempotency/IdempotencyKeyHasher.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/DuplicateKeyExceptionDetector.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/EntityConfigurations/IdempotencyRecordEntityTypeConfiguration.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Repositories/IdempotencyRepository.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/20260506075656_AddIdempotencyRecordSupport.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/20260506075656_AddIdempotencyRecordSupport.Designer.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/IdempotencyTests.cs`
- 新增：`检查台账/PR-长期数据库底座K-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Application/Services/Parcels/CreateParcelCommandService.cs`
- 修改：`Zeye.Sorting.Hub.Domain/Repositories/Models/Results/RepositoryErrorCodes.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/Routing/ParcelAdminApiRouteExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host.Tests/ParcelAdminApiTests.cs`
- 修改：`Zeye.Sorting.Hub.Host.Tests/ParcelBufferedWriteTests.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/SortingHubDbContextModelSnapshot.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Repositories/ParcelRepository.cs`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-K 断点摘要

### 已完成
- 幂等记录聚合
- 幂等仓储
- 幂等 Guard 服务
- 写入去重基础能力

### 保留能力
- 幂等键固定由 `SourceSystem + OperationName + BusinessKey + PayloadHash(SHA256)` 组成
- 管理端新增包裹入口已支持重复请求回放、处理中拒绝与取消后重试
- 唯一键冲突检测逻辑已抽取为共享工具，避免重复实现

### 未完成但已预留
- PR-L Outbox 事件底座与业务事件持久化
- 后续可将同一幂等 Guard 接入更多写命令与后台写入链路

### 下一 PR 入口
- 下一 PR 从 PR-L“Outbox 事件底座与业务事件持久化”开始
- 后续不要重复实现载荷 SHA256 计算与幂等状态流转，应复用 `IdempotencyGuardService` 与 `IdempotencyKeyHasher`

## 本次更新内容（长期数据库底座 PR-J：查询模板治理与索引建议闭环）

### 背景
在 PR-I 已补齐慢查询指纹聚合、窗口画像快照与诊断 API 后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-J。执行前先核对现有台账，确认 PR-A~PR-I 已完成，因此本次集中补齐查询模板登记、模板与慢查询画像匹配、只读索引建议报告与未登记缺口暴露。

### 新增内容

1. **查询模板登记底座**
   - 新增 `QueryTemplateDescriptor`、`QueryTemplateRegistry`。
   - 集中登记 `ParcelRecentCursorQuery`、`ParcelByBarcodeQuery`、`ParcelByChuteQuery`、`ParcelByWorkstationQuery`、`WebRequestAuditLogCursorQuery`、`ArchiveTaskListQuery` 六个模板。

2. **查询治理报告与索引建议**
   - 新增 `QueryIndexRecommendation`、`QueryGovernanceReport`。
   - 新增 `QueryIndexRecommendationService`，复用 `ISlowQueryProfileReader` 对当前慢查询画像做模板匹配、缺口识别与只读索引建议输出。

3. **Host 运行期接线**
   - 新增 `QueryGovernanceReportHostedService`。
   - 调整 `Program.cs` 与 `PersistenceServiceCollectionExtensions.cs`，接入查询治理报告后台服务与 DI 注册。
   - 在 `appsettings.json` 中新增 `Persistence:AutoTuning:QueryGovernance` 配置节，约束报告间隔、建议 P99 阈值与最小调用次数。

4. **测试与文档同步**
   - 新增 `QueryGovernanceTests.cs`，覆盖强制模板登记、已登记模板建议输出与未登记慢查询缺口暴露。
   - 新增 `检查台账/PR-长期数据库底座J-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/QueryGovernance/QueryTemplateDescriptor.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/QueryGovernance/QueryTemplateRegistry.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/QueryGovernance/QueryIndexRecommendation.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/QueryGovernance/QueryIndexRecommendationService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/QueryGovernance/QueryGovernanceReport.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/QueryGovernanceReportHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/QueryGovernanceTests.cs`
- 新增：`检查台账/PR-长期数据库底座J-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-J 断点摘要

### 已完成
- 查询模板登记
- 索引建议模型
- 查询治理报告

### 保留能力
- 索引建议仅输出日志与报告，不自动执行 DDL
- 查询治理报告复用慢查询画像只读抽象，不触发数据库重查询
- 未登记模板覆盖的慢查询指纹会显式暴露，便于后续门禁继续收紧

### 未完成但已预留
- PR-K 写入幂等、去重与重复键治理
- 后续可按 Provider 细化索引建议表达式与覆盖列策略

### 下一 PR 入口
- 下一 PR 从 PR-K“写入幂等、去重与重复键治理”开始
- 后续不要重复维护高频查询模板清单，应继续复用 `QueryTemplateRegistry`
## 本次更新内容（长期数据库底座 PR-I：慢查询指纹聚合与查询画像）

### 背景
在 PR-H 已补齐基线数据校验、配置一致性校验与可选种子入口后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-I。执行前先核对现有台账，确认 PR-A~PR-H 已完成，因此本次集中补齐慢查询 SQL 指纹去参数化、窗口聚合画像快照与只读诊断 API。

### 新增内容

1. **慢查询指纹聚合底座**
   - 新增 `SlowQueryFingerprint`、`SlowQueryFingerprintAggregator`。
   - 新增 `SlowQueryProfileSnapshot`、`SlowQueryProfileStore`。

2. **应用层诊断抽象与查询服务**
   - 新增 `ISlowQueryProfileReader`、`SlowQueryProfileReadModel`。
   - 新增 `GetSlowQueryProfileQueryService`，保证 Application 通过抽象读取画像快照。

3. **诊断合同与 API**
   - 新增 `SlowQueryProfileResponse`、`SlowQueryProfileListResponse`。
   - 新增 `DiagnosticsApiRouteExtensions`，暴露 `/api/diagnostics/slow-queries` 与 `/api/diagnostics/slow-queries/{fingerprint}`。

4. **现有链路复用与配置接线**
   - 调整 `SlowQueryCommandInterceptor`，在原有自动调优采集之外同步写入画像快照存储。
   - 调整 `SlowQueryAutoTuningPipeline` 复用共享指纹聚合器。
   - 在 `appsettings.json` 中新增 `Persistence:AutoTuning:SlowQueryProfile` 配置节。
   - 根据审查意见补强 SQL 单引号转义处理、样例 SQL 脱敏与单指纹样本上限控制。

5. **测试与文档同步**
   - 新增 `SlowQueryFingerprintTests.cs`，覆盖指纹归一化、窗口聚合、容量淘汰与查询服务读取。
   - 新增 `检查台账/PR-长期数据库底座I-检查台账.md`。
   - 同步更新 `README.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Application/Abstractions/Diagnostics/ISlowQueryProfileReader.cs`
- 新增：`Zeye.Sorting.Hub.Application/Abstractions/Diagnostics/SlowQueryProfileReadModel.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Diagnostics/GetSlowQueryProfileQueryService.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Diagnostics/SlowQueryProfileListResponse.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Diagnostics/SlowQueryProfileResponse.cs`
- 新增：`Zeye.Sorting.Hub.Host/Routing/DiagnosticsApiRouteExtensions.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/SlowQueryFingerprintTests.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryFingerprint.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryFingerprintAggregator.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryProfileSnapshot.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryProfileStore.cs`
- 新增：`检查台账/PR-长期数据库底座I-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryAutoTuningPipeline.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/AutoTuning/SlowQueryCommandInterceptor.cs`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-I 断点摘要

### 已完成
- 慢查询指纹聚合
- 查询画像快照
- 诊断 API

### 保留能力
- 查询画像仅读取进程内窗口快照，不访问数据库
- 画像快照只保留最近窗口，避免内存无限增长
- 指纹生成继续保持本地时间语义与中文日志约束

### 未完成但已预留
- PR-J 查询模板治理与索引建议闭环
- 后续可把画像快照升级为可选轻量持久化缓存

### 下一 PR 入口
- 下一 PR 从 PR-J“查询模板治理与索引建议闭环”开始
- 后续不要重复实现 SQL 去参数化与指纹计算，应复用 `SlowQueryFingerprintAggregator`

## 本次更新内容（长期数据库底座 PR-H：种子数据、基线数据与配置一致性校验）

### 背景
在 PR-G 已补齐迁移治理预演、危险迁移识别与回滚资产归档后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-H。当前切片聚焦启动期基线数据、默认参考数据目录与配置一致性校验，并补一个可选的幂等种子入口，为后续业务真实默认数据落库预留统一接入点。

### 新增内容

1. **基线数据目录与模型**
   - 新增 `BaselineDataOptions`、`BaselineDataValidationResult`。
   - 新增 `BaselineDataValidator` 与 `BaselineDataSeeder`。

2. **启动期基线校验与健康检查**
   - 新增 `BaselineDataValidationHostedService`。
   - 新增 `BaselineDataHealthCheck` 并接入 `/health/ready`。

3. **基线校验范围**
   - 必要配置节点存在性。
   - Provider 与连接字符串匹配。
   - `Persistence:Sharding:ParcelStartTime` 本地时间语义与时区后缀拦截。
   - 关键枚举 `Description` 完整性。
   - 默认参考数据目录非空与重复检查。
   - 表名/字段名映射配置当前未开放时输出中文提示。

4. **测试与稳定性修复**
   - 新增 `BaselineDataTests.cs`，覆盖校验成功/失败、时区后缀拦截、健康检查、种子入口与异常隔离。
   - 修复 `AutoTuningProductionControlTests` 中无效连接字符串示例，恢复当前分支测试稳定通过。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座H-检查台账.md`。
   - 同步更新 `README.md`、`数据库底座门禁说明.md`、`检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Baseline/BaselineDataOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Baseline/BaselineDataValidationResult.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Baseline/BaselineDataValidator.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Baseline/BaselineDataSeeder.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/BaselineDataValidationHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/BaselineDataHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/BaselineDataTests.cs`
- 新增：`检查台账/PR-长期数据库底座H-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`Zeye.Sorting.Hub.Host.Tests/AutoTuningProductionControlTests.cs`
- 修改：`README.md`
- 修改：`数据库底座门禁说明.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-H 断点摘要

### 已完成
- 基线数据校验
- 配置一致性校验
- 可选幂等种子数据入口

### 保留能力
- 默认仍以“只校验、不写入”为主，需显式开启 `IsSeedEnabled` 才会触发种子入口
- 当前种子入口为幂等 no-op，后续真实默认数据落库可在同一入口演进
- 所有新增时间相关校验继续保持本地时间语义，不引入 UTC/offset

### 未完成但已预留
- PR-I 慢查询指纹聚合与查询画像
- 后续可将默认参考数据目录升级为真实持久化默认数据写入与审计回滚链路

### 下一 PR 入口
- 下一 PR 从 PR-I“慢查询指纹聚合与查询画像”开始
- 后续不要重复实现基础配置/时间语义/枚举 Description 基线校验，应复用当前 BaselineDataValidator

## 本次更新内容（长期数据库底座 PR-G：数据库迁移治理与回滚资产）

### 背景
在 PR-A/B/C/D/E/F 已分别补齐数据库连接诊断、查询保护、批量缓冲写入、分表治理、归档 dry-run 与数据库底座 CI 门禁后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-G。执行前先核对当前长期数据库底座完成度，确认仓库已完成到 PR-F，因此本次集中补齐迁移治理预演、危险迁移识别、脚本归档、回滚参考资产与健康检查联动。

### 新增内容

1. **迁移治理基础模型与服务**
   - 新增 `MigrationPlan`、`MigrationExecutionRecord`、`MigrationGovernanceStateStore`。
   - 新增 `MigrationSafetyEvaluator`、`MigrationScriptArchiveService`、`MigrationRollbackScriptProvider`。

2. **启动期迁移治理预演**
   - 新增 `MigrationGovernanceHostedService`。
   - 启动时读取全部/已应用/待执行 EF 迁移，生成迁移脚本、识别危险 SQL，并归档正向脚本与人工回滚参考脚本。

3. **Host 接线与迁移联动**
   - 新增 `MigrationGovernanceHealthCheck` 并接入 `/health/ready`。
   - 调整 `DatabaseInitializerHostedService`，复用迁移治理预演结果，支持 dry-run 与生产危险迁移阻断时跳过真实迁移。
   - `appsettings.json` 新增 `Persistence:MigrationGovernance` 配置节。

4. **自动化测试补齐**
   - 新增 `MigrationGovernanceTests.cs`，覆盖健康检查、危险 SQL、dry-run 决策、脚本归档与预演异常记录。

5. **台账与文档同步**
   - 新增 `检查台账/PR-长期数据库底座G-检查台账.md`。
   - 同步更新 `README.md` 与 `检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationPlan.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationExecutionRecord.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationGovernanceStateStore.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationScriptArchiveService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationRollbackScriptProvider.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationSafetyEvaluator.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/MigrationGovernanceHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/MigrationGovernanceHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/MigrationGovernanceTests.cs`
- 新增：`检查台账/PR-长期数据库底座G-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/HostedServices/DatabaseInitializerHostedService.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-G 断点摘要

### 已完成
- 迁移计划生成
- 迁移脚本归档
- 危险迁移识别
- 迁移治理健康检查

### 新增文件
- `Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationPlan.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationExecutionRecord.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationGovernanceStateStore.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationScriptArchiveService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationRollbackScriptProvider.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/MigrationGovernance/MigrationSafetyEvaluator.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/MigrationGovernanceHostedService.cs`
- `Zeye.Sorting.Hub.Host/HealthChecks/MigrationGovernanceHealthCheck.cs`
- `Zeye.Sorting.Hub.Host.Tests/MigrationGovernanceTests.cs`
- `检查台账/PR-长期数据库底座G-检查台账.md`

### 修改文件
- `Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/DatabaseInitializerHostedService.cs`
- `Zeye.Sorting.Hub.Host/Program.cs`
- `Zeye.Sorting.Hub.Host/appsettings.json`
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- 回滚脚本仅作为人工审核与归档参考，不自动执行不可逆回滚
- 当前迁移治理已与 `DatabaseInitializerHostedService` 联动，避免重复迁移检测

### 未完成但已预留
- PR-H 种子数据、基线数据与配置一致性校验
- 后续可将迁移脚本归档接入 CI artifact / DBA 审批链路

### 下一 PR 入口
- 下一 PR 从 PR-H“种子数据、基线数据与配置一致性校验”开始
- 后续不要重复实现迁移检测，应复用当前迁移治理状态存储、脚本归档与危险 SQL 识别能力

## 历史更新内容（长期数据库底座 PR-F：数据库底座 CI 门禁增强）

### 背景
在 PR-A/B/C/D/E 已分别补齐数据库连接诊断、查询保护、批量缓冲写入、分表治理与归档 dry-run 后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-F。执行前先核对当前长期数据库底座完成度，确认仓库已完成到 PR-E，PR-F 仅由既有 `stability-gates.yml` 与 `copilot-instructions-validation.yml` 局部覆盖，因此本次补齐数据库底座专用 CI 门禁工作流与规则脚本。

### 新增内容

1. **数据库底座门禁工作流**
   - 新增 `.github/workflows/database-foundation-gates.yml`。
   - 固定执行 `dotnet restore`、Release 构建与 Release 测试，并拆分数据库底座规则门禁步骤。

2. **数据库底座规则脚本**
   - 新增 `.github/scripts/validate-database-foundation-rules.sh`。
   - 新增 `validate-no-utc.sh`、`validate-readme-file-tree.sh`、`validate-sensitive-config.sh`、`validate-no-shadow-code.sh` 四个薄包装脚本，复用主脚本能力，避免脚本层影分身代码。

3. **规则覆盖范围**
   - 新增 UTC API / 配置时区后缀增量拦截。
   - 新增 README 文件树与职责说明增量对账。
   - 新增敏感配置、影分身代码、枚举 Description、HostedService 异常捕获、后台循环 CancellationToken 与有界容量校验。

4. **台账与说明同步**
   - 新增 `数据库底座门禁说明.md`。
   - 新增 `检查台账/PR-长期数据库底座F-检查台账.md`。
   - 同步更新 `README.md` 与 `检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`.github/workflows/database-foundation-gates.yml`
- 新增：`.github/scripts/validate-database-foundation-rules.sh`
- 新增：`.github/scripts/validate-no-utc.sh`
- 新增：`.github/scripts/validate-readme-file-tree.sh`
- 新增：`.github/scripts/validate-sensitive-config.sh`
- 新增：`.github/scripts/validate-no-shadow-code.sh`
- 新增：`数据库底座门禁说明.md`
- 新增：`检查台账/PR-长期数据库底座F-检查台账.md`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-F 断点摘要

### 已完成
- 数据库底座 CI 门禁工作流
- UTC / 配置时区后缀增量拦截
- README 文件树与职责说明增量对账
- 敏感配置与影分身代码增量拦截
- 枚举 Description、HostedService 异常捕获、后台循环取消、有界容量结构性检查

### 新增文件
- `.github/workflows/database-foundation-gates.yml`
- `.github/scripts/validate-database-foundation-rules.sh`
- `.github/scripts/validate-no-utc.sh`
- `.github/scripts/validate-readme-file-tree.sh`
- `.github/scripts/validate-sensitive-config.sh`
- `.github/scripts/validate-no-shadow-code.sh`
- `数据库底座门禁说明.md`
- `检查台账/PR-长期数据库底座F-检查台账.md`

### 修改文件
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- `stability-gates.yml` 与 `copilot-instructions-validation.yml` 保持兼容
- 当前数据库底座门禁默认基于增量 diff 扫描，不强制一次性清理历史遗留风险

### 未完成但已预留
- PR-G 数据库迁移治理与回滚资产
- 后续可将敏感配置与 README 对账升级为更强语义级校验

### 下一 PR 入口
- 下一 PR 从 PR-G“数据库迁移治理与回滚资产”开始
- 后续迁移治理需复用当前数据库底座门禁工作流，不新增平行重复校验链路

## 本次更新内容（长期数据库底座 PR-E：数据归档与冷热分层）

### 背景
在 PR-A/B/C/D 已分别补齐数据库连接诊断、查询保护、批量缓冲写入与分表治理后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-E。当前仓库缺少可追踪、可重试的归档任务实体与后台 dry-run 执行骨架，因此本次补齐数据归档与冷热分层的最小可落地底座。

### 新增内容

1. **归档任务领域与应用服务**
   - 新增 `ArchiveTask` 聚合、`ArchiveTaskStatus` / `ArchiveTaskType` 枚举与 `IArchiveTaskRepository`。
   - 新增 `CreateArchiveTaskCommandService`、`GetArchiveTaskPagedQueryService`、`RetryArchiveTaskCommandService` 与 `ArchiveTaskContractMapper`。

2. **归档 dry-run 基础设施**
   - 新增 `DataArchiveOptions`、`DataArchivePlanner`、`DataArchiveCheckpointStore`、`DataArchiveExecutor`、`DataArchiveHostedWorker`。
   - 新增 `ArchiveTaskRepository` 与 `ArchiveTaskEntityTypeConfiguration`。
   - 新增 `ArchiveTasks` 迁移与模型快照更新。

3. **Host 接线与接口**
   - 新增 `DataArchiveHostedService`。
   - 新增 `DataGovernanceApiRouteExtensions`，提供创建、分页查询与重试接口。
   - `appsettings.json` 新增 `Persistence:Archiving` 配置节。

4. **自动化测试补齐**
   - 新增 `DataArchiveTaskTests.cs`，覆盖创建、分页、后台执行、重试与非法类型。

5. **台账与文档同步**
   - 新增 `检查台账/PR-长期数据库底座E-检查台账.md`。
   - 同步更新 `README.md` 与 `检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Domain/Aggregates/DataGovernance/ArchiveTask.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Enums/DataGovernance/ArchiveTaskStatus.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Enums/DataGovernance/ArchiveTaskType.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/IArchiveTaskRepository.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/DataGovernance/ArchiveTaskContractMapper.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/DataGovernance/CreateArchiveTaskCommandService.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/DataGovernance/GetArchiveTaskPagedQueryService.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/DataGovernance/RetryArchiveTaskCommandService.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskCreateRequest.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskListRequest.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskListResponse.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskResponse.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchivePlanner.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveCheckpointStore.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveExecutor.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveHostedWorker.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Repositories/ArchiveTaskRepository.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/EntityConfigurations/ArchiveTaskEntityTypeConfiguration.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/AddArchiveTaskDryRunSupport.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/AddArchiveTaskDryRunSupportDesigner.cs`
- 新增：`Zeye.Sorting.Hub.Host/Routing/DataGovernanceApiRouteExtensions.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/DataArchiveHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/DataArchiveTaskTests.cs`
- 新增：`检查台账/PR-长期数据库底座E-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/SortingHubDbContextModelSnapshot.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`README.md`
- 修改：`更新记录.md`
- 修改：`检查台账/文件清单基线.txt`

## PR-E 断点摘要

### 已完成
- 归档任务聚合与仓储
- 归档 dry-run 计划器与后台执行骨架
- 归档任务创建 / 分页 / 重试 API
- 归档任务迁移、测试、README 与台账同步

### 新增文件
- `Zeye.Sorting.Hub.Domain/Aggregates/DataGovernance/ArchiveTask.cs`
- `Zeye.Sorting.Hub.Domain/Enums/DataGovernance/ArchiveTaskStatus.cs`
- `Zeye.Sorting.Hub.Domain/Enums/DataGovernance/ArchiveTaskType.cs`
- `Zeye.Sorting.Hub.Domain/Repositories/IArchiveTaskRepository.cs`
- `Zeye.Sorting.Hub.Application/Services/DataGovernance/ArchiveTaskContractMapper.cs`
- `Zeye.Sorting.Hub.Application/Services/DataGovernance/CreateArchiveTaskCommandService.cs`
- `Zeye.Sorting.Hub.Application/Services/DataGovernance/GetArchiveTaskPagedQueryService.cs`
- `Zeye.Sorting.Hub.Application/Services/DataGovernance/RetryArchiveTaskCommandService.cs`
- `Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskCreateRequest.cs`
- `Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskListRequest.cs`
- `Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskListResponse.cs`
- `Zeye.Sorting.Hub.Contracts/Models/DataGovernance/ArchiveTaskResponse.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveOptions.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchivePlanner.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveCheckpointStore.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveExecutor.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Archiving/DataArchiveHostedWorker.cs`
- `Zeye.Sorting.Hub.Infrastructure/Repositories/ArchiveTaskRepository.cs`
- `Zeye.Sorting.Hub.Infrastructure/EntityConfigurations/ArchiveTaskEntityTypeConfiguration.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/AddArchiveTaskDryRunSupport.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/AddArchiveTaskDryRunSupportDesigner.cs`
- `Zeye.Sorting.Hub.Host/Routing/DataGovernanceApiRouteExtensions.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/DataArchiveHostedService.cs`
- `Zeye.Sorting.Hub.Host.Tests/DataArchiveTaskTests.cs`
- `检查台账/PR-长期数据库底座E-检查台账.md`

### 修改文件
- `Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Migrations/SortingHubDbContextModelSnapshot.cs`
- `Zeye.Sorting.Hub.Host/Program.cs`
- `Zeye.Sorting.Hub.Host/appsettings.json`
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- 当前仅执行 dry-run 计划与审计记录，不执行真实删除、迁移或压缩导出
- PR-A/PR-B/PR-C/PR-D 已交付能力保持兼容

### 未完成但已预留
- PR-F 数据库底座 CI 门禁增强
- 后续真实冷库迁移执行器、危险动作隔离器、审计与回滚资产

### 下一 PR 入口
- 下一 PR 从 PR-F“数据库底座 CI 门禁增强”开始
- 不要绕过当前归档 dry-run 状态流转与检查点模型

## 本次更新内容（长期数据库底座 PR-D：分表巡检、预建与索引检查）

### 背景
在 PR-A/B/C 已分别补齐数据库连接诊断、查询保护与批量缓冲写入后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-D。当前仓库已有启动期分表守卫与 Provider 元数据探测接口，但缺少运行期巡检服务、预建计划服务与独立健康检查，因此本次补齐分表治理运行期底座。

### 新增内容

1. **分表巡检与预建模型**
   - 新增 `ShardingRuntimeInspectionOptions`、`ShardingPrebuildOptions`。
   - 新增 `ShardingInspectionReport`、`ShardingPrebuildPlan`。

2. **分表治理基础设施服务**
   - 新增 `ShardingPhysicalTablePlanBuilder`，统一生成当前、下一周期与未来窗口内的物理分表名。
   - 新增 `ShardingTableInspectionService`，覆盖缺表、关键索引、容量/热点风险与 WebRequestAuditLog 热表/详情表一致性检查。
   - 新增 `ShardingTablePrebuildService`，默认只输出 dry-run 预建计划，不执行真实 DDL。
   - 新增 `ShardingIndexInspectionService` 与 `ShardingCapacitySnapshotService`。

3. **Host 接线与健康检查**
   - 新增 `ShardingInspectionHostedService`、`ShardingPrebuildHostedService`。
   - 新增 `ShardingGovernanceHealthCheck`，并接入 `/health/ready`。
   - `appsettings.json` 新增 `Persistence:Sharding:RuntimeInspection` 与 `Persistence:Sharding:Prebuild` 配置节。

4. **自动化测试补齐**
   - 新增 `ConfigurableShardingPhysicalTableProbe`。
   - 新增 `ShardingInspectionTests.cs`，覆盖物理表规划、预建 dry-run、索引缺失、缺表巡检与健康检查状态。

5. **台账与文档同步**
   - 新增 `检查台账/PR-长期数据库底座D-检查台账.md`。
   - 同步更新 `README.md` 与 `检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingRuntimeInspectionOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingPrebuildOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingInspectionReport.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingPrebuildPlan.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingPhysicalTablePlanBuilder.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingCapacitySnapshotService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingIndexInspectionService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingTableInspectionService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingTablePrebuildService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/ShardingInspectionHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/ShardingPrebuildHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/ShardingGovernanceHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/ConfigurableShardingPhysicalTableProbe.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/ShardingInspectionTests.cs`
- 新增：`检查台账/PR-长期数据库底座D-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`README.md`

## PR-D 断点摘要

### 已完成
- 分表物理表巡检
- 分表预建计划
- 分表索引检查
- 分表治理健康检查

### 新增文件
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingRuntimeInspectionOptions.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingPrebuildOptions.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingInspectionReport.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingPrebuildPlan.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingPhysicalTablePlanBuilder.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingCapacitySnapshotService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingIndexInspectionService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingTableInspectionService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Sharding/ShardingTablePrebuildService.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/ShardingInspectionHostedService.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/ShardingPrebuildHostedService.cs`
- `Zeye.Sorting.Hub.Host/HealthChecks/ShardingGovernanceHealthCheck.cs`
- `Zeye.Sorting.Hub.Host.Tests/ConfigurableShardingPhysicalTableProbe.cs`
- `Zeye.Sorting.Hub.Host.Tests/ShardingInspectionTests.cs`
- `检查台账/PR-长期数据库底座D-检查台账.md`

### 修改文件
- `Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- `Zeye.Sorting.Hub.Host/Program.cs`
- `Zeye.Sorting.Hub.Host/appsettings.json`
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- 既有 DatabaseInitializerHostedService 分表守卫与自动迁移链路保持兼容
- 真实预建 DDL 尚未开放，仍以 dry-run 计划优先

### 未完成但已预留
- PR-E 数据归档与冷热分层
- 后续真实预建执行需接入危险动作隔离器、审计与回滚说明后再开放

### 下一 PR 入口
- 下一 PR 从 PR-E“数据归档与冷热分层”开始
- 不要重复实现分表探测

## 本次更新内容（长期数据库底座 PR-C：批量写入缓冲与死信隔离）

### 背景
在 PR-A 已补齐数据库连接诊断与详细就绪探针、PR-B 已补齐查询保护与游标分页后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-C。当前 Parcel 写入链路只有同步强一致接口，缺少有界缓冲、后台批量 Flush、死信隔离与队列健康检查，因此本次补齐批量缓冲写入底座并保持同步新增接口不变。

### 新增内容

1. **批量缓冲写入应用抽象与请求映射**
   - 新增 `BufferedWriteOptions`、`BufferedWriteResult`、`IBufferedWriteService`。
   - 新增 `ParcelCreateRequestMapper`，统一同步新增与批量缓冲写入的聚合构建逻辑。

2. **批量缓冲写入基础设施落地**
   - 新增 `BoundedWriteChannel<T>`、`BufferedParcelWriteItem`、`ParcelBufferedWriteService`、`ParcelBatchWriteFlushService`。
   - 新增 `DeadLetterWriteEntry`、`DeadLetterWriteStore`、`BatchWriteMetricsSnapshot`，补齐失败隔离与队列观测。

3. **Host 接线与健康检查**
   - 新增 `ParcelBatchWriteFlushHostedService`。
   - 新增 `BufferedWriteQueueHealthCheck`，并接入 `/health/ready`。
   - `ParcelAdminApiRouteExtensions` 新增 `POST /api/admin/parcels/batch-buffer`。
   - `appsettings.json` 新增 `Persistence:WriteBuffering` 配置节。

4. **契约与测试补齐**
   - 新增 `ParcelBatchBufferedCreateRequest`、`ParcelBatchBufferedCreateResponse`。
   - 新增 `ParcelBufferedWriteTests.cs`，覆盖入队、回压、批量 Flush、死信隔离、健康检查与 batch-buffer 接口。
   - 扩展 `FakeParcelRepository` 支持批量写入失败场景。

5. **文档与台账同步**
   - 新增 `检查台账/PR-长期数据库底座C-检查台账.md`。
   - 同步更新 `README.md` 与 `检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Application/Mappers/Parcels/ParcelCreateRequestMapper.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/WriteBuffers/BufferedWriteOptions.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/WriteBuffers/BufferedWriteResult.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/WriteBuffers/IBufferedWriteService.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Parcels/Admin/ParcelBatchBufferedCreateRequest.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Parcels/Admin/ParcelBatchBufferedCreateResponse.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/ParcelBatchWriteFlushHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/BufferedWriteQueueHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/BatchWriteMetricsSnapshot.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/BoundedWriteChannel.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/BufferedParcelWriteItem.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/DeadLetterWriteEntry.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/DeadLetterWriteStore.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/ParcelBatchWriteFlushService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/ParcelBufferedWriteService.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/ParcelBufferedWriteTests.cs`
- 新增：`检查台账/PR-长期数据库底座C-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Application/Services/Parcels/CreateParcelCommandService.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Zeye.Sorting.Hub.Infrastructure.csproj`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/Routing/ParcelAdminApiRouteExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`Zeye.Sorting.Hub.Host.Tests/FakeParcelRepository.cs`
- 修改：`README.md`

## PR-C 断点摘要

### 已完成
- Parcel 批量缓冲写入
- 有界 Channel
- Flush 后台服务
- 死信隔离
- 写入队列健康检查

### 新增文件
- `Zeye.Sorting.Hub.Application/Mappers/Parcels/ParcelCreateRequestMapper.cs`
- `Zeye.Sorting.Hub.Application/Services/WriteBuffers/BufferedWriteOptions.cs`
- `Zeye.Sorting.Hub.Application/Services/WriteBuffers/BufferedWriteResult.cs`
- `Zeye.Sorting.Hub.Application/Services/WriteBuffers/IBufferedWriteService.cs`
- `Zeye.Sorting.Hub.Contracts/Models/Parcels/Admin/ParcelBatchBufferedCreateRequest.cs`
- `Zeye.Sorting.Hub.Contracts/Models/Parcels/Admin/ParcelBatchBufferedCreateResponse.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/ParcelBatchWriteFlushHostedService.cs`
- `Zeye.Sorting.Hub.Host/HealthChecks/BufferedWriteQueueHealthCheck.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/BatchWriteMetricsSnapshot.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/BoundedWriteChannel.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/BufferedParcelWriteItem.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/DeadLetterWriteEntry.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/DeadLetterWriteStore.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/ParcelBatchWriteFlushService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/WriteBuffering/ParcelBufferedWriteService.cs`
- `Zeye.Sorting.Hub.Host.Tests/ParcelBufferedWriteTests.cs`
- `检查台账/PR-长期数据库底座C-检查台账.md`

### 修改文件
- `Zeye.Sorting.Hub.Application/Services/Parcels/CreateParcelCommandService.cs`
- `Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- `Zeye.Sorting.Hub.Infrastructure/Zeye.Sorting.Hub.Infrastructure.csproj`
- `Zeye.Sorting.Hub.Host/Program.cs`
- `Zeye.Sorting.Hub.Host/Routing/ParcelAdminApiRouteExtensions.cs`
- `Zeye.Sorting.Hub.Host/appsettings.json`
- `Zeye.Sorting.Hub.Host.Tests/FakeParcelRepository.cs`
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- `POST /api/admin/parcels` 仍为同步强一致新增接口
- PR-A/PR-B 已交付能力保持兼容

### 未完成但已预留
- PR-D 分表巡检、预建与索引检查
- 后续可在不破坏现有接口前提下扩展死信查询与治理能力

### 下一 PR 入口
- 下一 PR 从 PR-D“分表巡检、预建与索引检查”开始
- 不要把同步新增接口改成缓冲写入

## 本次更新内容（长期数据库底座 PR-B：查询保护与游标分页）

### 背景
在 PR-A 已补齐数据库连接诊断与详细就绪探针后，继续按《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》推进 PR-B。当前 Parcel 只读链路仅支持偏移分页，缺少游标分页与查询保护，因此本次收口游标分页、默认时间窗口与最大页码保护，并同步更新路线图断点。

### 新增内容

1. **游标分页领域与合同模型落地**
   - 新增 `CursorPageRequest`、`CursorPageResult`，在 Domain 层统一描述游标分页请求与结果。
   - 新增 `ParcelCursorToken`、`ParcelCursorListRequest`、`ParcelCursorListResponse`，在 Contracts 层提供游标分页 API 契约。

2. **Parcel 查询保护与游标分页仓储实现**
   - `IParcelRepository` 新增 `GetCursorPagedAsync`。
   - `ParcelRepository` 新增游标分页实现，复用现有过滤、验证与读模型投影，只切换分页方式。
   - 新增 `ParcelCursorQueryExtensions`，集中封装 `ScannedTime DESC, Id DESC` 稳定排序下的游标条件，避免影分身代码。

3. **Application 与 Host 接线**
   - 新增 `ParcelQueryRequestMapper`，统一普通分页与游标分页的默认最近 24 小时时间窗口和过滤映射。
   - 新增 `GetParcelCursorPagedQueryService`。
   - `ParcelReadOnlyApiRouteExtensions` 新增 `/api/parcels/cursor`，并为普通 `/api/parcels` 增加默认时间范围与最大页码说明。
   - `Program.cs` 注册游标分页查询服务。

4. **普通分页保护增强**
   - `GetParcelPagedQueryService` 增加最大页码 10000 保护。
   - 普通分页与游标分页在未指定时间范围时，默认仅查询最近 24 小时。

5. **自动化测试补齐**
   - 新增 `ParcelCursorQueryTests.cs`，覆盖首页、翻页、非法游标、页大小归一化、默认最近 24 小时、普通分页页码保护与仓储稳定排序。
   - `FakeParcelRepository`、`ParcelReadOnlyApiTests` 同步适配游标分页链路。

6. **台账与文档同步**
   - 新增 `检查台账/PR-长期数据库底座B-检查台账.md`，记录 PR-B 完成度、交付清单、验证结果与下一 PR 入口。
   - 同步更新 `README.md` 与 `检查台账/文件清单基线.txt`。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/Models/Paging/CursorPageRequest.cs`
- 新增：`Zeye.Sorting.Hub.Domain/Repositories/Models/Paging/CursorPageResult.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Parcels/ParcelCursorToken.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Parcels/ParcelCursorListRequest.cs`
- 新增：`Zeye.Sorting.Hub.Contracts/Models/Parcels/ParcelCursorListResponse.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Parcels/ParcelQueryRequestMapper.cs`
- 新增：`Zeye.Sorting.Hub.Application/Services/Parcels/GetParcelCursorPagedQueryService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Repositories/ParcelCursorQueryExtensions.cs`
- 新增：`Zeye.Sorting.Hub.Host/QueryParameters/ParcelCursorListQueryParameters.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/ParcelCursorQueryTests.cs`
- 新增：`检查台账/PR-长期数据库底座B-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Domain/Repositories/IParcelRepository.cs`
- 修改：`Zeye.Sorting.Hub.Application/Services/Parcels/GetParcelPagedQueryService.cs`
- 修改：`Zeye.Sorting.Hub.Infrastructure/Repositories/ParcelRepository.cs`
- 修改：`Zeye.Sorting.Hub.Host/Routing/ParcelReadOnlyApiRouteExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host.Tests/FakeParcelRepository.cs`
- 修改：`Zeye.Sorting.Hub.Host.Tests/ParcelReadOnlyApiTests.cs`
- 修改：`README.md`

## PR-B 断点摘要

### 已完成
- 游标分页领域模型、合同模型与游标令牌
- Parcel 仓储游标分页实现与游标查询扩展
- Application 游标查询服务与 `/api/parcels/cursor` 路由
- 普通分页默认最近 24 小时与最大页码 10000 保护
- PR-B 对应测试、台账与 README 同步

### 新增文件
- `Zeye.Sorting.Hub.Domain/Repositories/Models/Paging/CursorPageRequest.cs`
- `Zeye.Sorting.Hub.Domain/Repositories/Models/Paging/CursorPageResult.cs`
- `Zeye.Sorting.Hub.Contracts/Models/Parcels/ParcelCursorToken.cs`
- `Zeye.Sorting.Hub.Contracts/Models/Parcels/ParcelCursorListRequest.cs`
- `Zeye.Sorting.Hub.Contracts/Models/Parcels/ParcelCursorListResponse.cs`
- `Zeye.Sorting.Hub.Application/Services/Parcels/ParcelQueryRequestMapper.cs`
- `Zeye.Sorting.Hub.Application/Services/Parcels/GetParcelCursorPagedQueryService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Repositories/ParcelCursorQueryExtensions.cs`
- `Zeye.Sorting.Hub.Host/QueryParameters/ParcelCursorListQueryParameters.cs`
- `Zeye.Sorting.Hub.Host.Tests/ParcelCursorQueryTests.cs`
- `检查台账/PR-长期数据库底座B-检查台账.md`

### 修改文件
- `Zeye.Sorting.Hub.Domain/Repositories/IParcelRepository.cs`
- `Zeye.Sorting.Hub.Application/Services/Parcels/GetParcelPagedQueryService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Repositories/ParcelRepository.cs`
- `Zeye.Sorting.Hub.Host/Routing/ParcelReadOnlyApiRouteExtensions.cs`
- `Zeye.Sorting.Hub.Host/Program.cs`
- `Zeye.Sorting.Hub.Host.Tests/FakeParcelRepository.cs`
- `Zeye.Sorting.Hub.Host.Tests/ParcelReadOnlyApiTests.cs`
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- 原有 `/api/parcels`、`/api/parcels/{id}`、`/api/parcels/adjacent` 接口保持兼容
- 原有偏移分页响应结构保持兼容，未改动 `ParcelListResponse`
- PR-A 数据库连接诊断、现有自动迁移、分表治理与自动调谐链路未被侵入式改动

### 未完成但已预留
- PR-C 批量写入缓冲与死信隔离
- PR-B 后续如需继续优化，可单独补充 `includeTotalCount=false` 的统计优化切片

### 下一 PR 入口
- 下一 PR 从 PR-C“批量写入缓冲与死信隔离”开始
- 禁止重复实现游标令牌编码、默认最近 24 小时窗口与稳定排序条件

## 本次更新内容（长期数据库底座 PR-A：数据库连接诊断与就绪状态增强）

### 背景
先核对《Zeye.Sorting.Hub-长期数据库底座多PR实施方案与Copilot严格门禁.md》与仓库现状：当前 PR-D（分表治理）、PR-F（CI 门禁）、PR-I（慢查询采集）仅部分完成，PR-A/B/C/E/G 及之后的底座能力仍未落地。本次选择最独立且最适合作为实施起点的 PR-A，先补齐数据库连接诊断、连接预热与详细就绪探针。

### 新增内容

1. **数据库连接诊断底座落地**
   - 新增 `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/` 目录，补齐 `DatabaseConnectionDiagnosticsOptions`、`DatabaseConnectionHealthSnapshot`、`IDatabaseConnectionDiagnostics`、`DatabaseConnectionDiagnosticsService`、`DatabaseConnectionWarmupService`。
   - 统一通过 `IDbContextFactory<SortingHubDbContext>` 执行短生命周期探测，缓存最近一次快照，记录 provider、database、CheckedAtLocal、ElapsedMilliseconds、连续失败/成功次数、恢复观察期与失败信息。

2. **详细数据库就绪探针与启动预热接线**
   - 新增 `DatabaseConnectionDetailedHealthCheck`，将 `/health/ready` 升级为详细数据库健康检查。
   - 新增 `DatabaseConnectionWarmupHostedService`，在宿主启动期按配置执行非阻塞连接预热，并确保所有异常经 NLog 记录。
   - `HealthCheckResponseWriter` 新增 `Data` 输出能力，健康检查 JSON 可直接返回诊断快照字段。

3. **配置与依赖注入补齐**
   - `PersistenceServiceCollectionExtensions` 新增数据库连接诊断选项注册、参数校验与服务注册。
   - `appsettings.json` 新增 `Persistence:Diagnostics` 节，明确预热开关、连接数、探测超时、失败阈值与恢复阈值。

4. **自动化测试补齐**
   - 新增 `DatabaseConnectionDiagnosticsTests.cs`，覆盖默认配置、非法配置、失败快照不外抛、失败阈值触发 Unhealthy、本地时间语义与健康检查关键 Data 输出，共 7 个用例。

5. **台账与文档同步**
   - 新增 `检查台账/PR-长期数据库底座A-检查台账.md`，记录现状核对、交付文件、验证结果与下一 PR 入口。
   - 同步更新 `README.md`，补充新增目录、文件职责与本次更新摘要。

### 变更文件
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionDiagnosticsOptions.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionHealthSnapshot.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/IDatabaseConnectionDiagnostics.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionDiagnosticsService.cs`
- 新增：`Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionWarmupService.cs`
- 新增：`Zeye.Sorting.Hub.Host/HealthChecks/DatabaseConnectionDetailedHealthCheck.cs`
- 新增：`Zeye.Sorting.Hub.Host/HostedServices/DatabaseConnectionWarmupHostedService.cs`
- 新增：`Zeye.Sorting.Hub.Host.Tests/DatabaseConnectionDiagnosticsTests.cs`
- 新增：`检查台账/PR-长期数据库底座A-检查台账.md`
- 修改：`Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- 修改：`Zeye.Sorting.Hub.Host/HealthChecks/HealthCheckResponseWriter.cs`
- 修改：`Zeye.Sorting.Hub.Host/Program.cs`
- 修改：`Zeye.Sorting.Hub.Host/appsettings.json`
- 修改：`README.md`

## PR-A 断点摘要

### 已完成
- 数据库连接诊断配置、快照与服务抽象
- 数据库连接预热服务与托管服务
- 详细数据库健康检查与健康检查 Data 输出
- PR-A 对应测试与台账

### 新增文件
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionDiagnosticsOptions.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionHealthSnapshot.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/IDatabaseConnectionDiagnostics.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionDiagnosticsService.cs`
- `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/DatabaseConnectionWarmupService.cs`
- `Zeye.Sorting.Hub.Host/HealthChecks/DatabaseConnectionDetailedHealthCheck.cs`
- `Zeye.Sorting.Hub.Host/HostedServices/DatabaseConnectionWarmupHostedService.cs`
- `Zeye.Sorting.Hub.Host.Tests/DatabaseConnectionDiagnosticsTests.cs`
- `检查台账/PR-长期数据库底座A-检查台账.md`

### 修改文件
- `Zeye.Sorting.Hub.Infrastructure/DependencyInjection/PersistenceServiceCollectionExtensions.cs`
- `Zeye.Sorting.Hub.Host/HealthChecks/HealthCheckResponseWriter.cs`
- `Zeye.Sorting.Hub.Host/Program.cs`
- `Zeye.Sorting.Hub.Host/appsettings.json`
- `README.md`
- `更新记录.md`
- `检查台账/文件清单基线.txt`

### 删除文件
- 无

### 保留能力
- 原有 `/health/live`、`/health` 兼容端点未变
- 原有 `DatabaseReadinessHealthCheck.cs` 文件保留，未直接删除
- 自动迁移、分表治理、自动调谐现有链路保持兼容

### 未完成但已预留
- PR-B 查询保护与游标分页
- PR-C 批量写入缓冲与死信隔离
- PR-A 后续如需兼容独立 `/health/details` 可再补充端点封装

### 下一 PR 入口
- 下一 PR 从 PR-B“查询保护与游标分页”开始
- 禁止重复实现数据库连接诊断、预热 HostedService 与详细健康检查
- 禁止删除 `Zeye.Sorting.Hub.Infrastructure/Persistence/Diagnostics/` 已落地基础能力

## 本次更新内容（持续运行一年稳定性改造清单 - 补充实施）

### 背景
通读全部代码后，验证 `长期运行优化与热更新支持清单.md` 所有主项已实现（全 [x]）。基于 `待完善事项.md` 中优先级最高的条目，补充以下稳定性治理实施：

### 新增内容

1. **演练记录目录与首份季度演练记录**
   - 新建 `drill-records/` 目录，添加 `2026-Q1-稳定性演练记录.md`，记录 7 个关键场景的演练过程与结果（数据库自动重试恢复、隔离器拦截、配置热更新、告警风暴抑制、分表策略切换门禁、月报自动生成、回滚资产验证）。

2. **演练记录门禁升级为强制阻断（门禁 8）**
   - `stability-gates.yml` 中 `drill-record-gate` 从警告模式（`exit 0`）升级为强制阻断（`exit 1`）。
   - 缺少 `drill-records/` 演练记录文件时，PR 将无法合并，真正落实"未达标禁止发布"。
   - 同步修复 `find` 命令中括号表达式语法（确保跨平台兼容）。

3. **新增门禁 9：分表预建校验门禁（sharding-prebuild-gate）**
   - 验证 `PrebuildWindowHours >= 24`（配置值合法性）。
   - 验证 `DatabaseInitializerHostedService` 中分表预建逻辑存在。
   - 验证 `ShardingGovernanceHitRateThreshold` 配置存在。
   - 实现了 `待完善事项.md` 中"分表治理 #1"的 CI 门禁需求。

4. **新增门禁 10：迁移脚本归档验证门禁（migration-archive-gate）**
   - 验证 `Migrations/` 目录存在且包含迁移文件。
   - 验证 `ModelSnapshot.cs` 文件存在。
   - 防止迁移资产误删导致版本升级失败。

5. **`config-validity` 门禁补充分表预建配置检查**
   - 新增 `check_key "PrebuildWindowHours"` 验证，确保分表预建窗口配置不缺失。

6. **workflow 触发路径补充**
   - `stability-gates.yml` `on.pull_request.paths` 新增 `drill-records/**`，演练记录变更时自动触发 CI。

### 变更文件
- 新增：`drill-records/2026-Q1-稳定性演练记录.md`
- 修改：`.github/workflows/stability-gates.yml`（门禁 8 升级 + 新增门禁 9/10 + 配置检查补充）
- 修改：`待完善事项.md`（标记分表治理 #1、迁移治理 #3、可观测性 #2 已实现）
- 修改：`长期运行优化与热更新支持清单.md`（补充新门禁描述）
- 修改：`README.md`（文件树新增 drill-records/，门禁数更新为 10 项）

## 本次更新内容（长期运行与热更新清单重构）

1. 新增 `长期运行优化与热更新支持清单.md`，将长期运行与热更新目标重构为“一年无人值守运行”导向的治理清单，覆盖稳定运行底座、热更新、热替换、阶段化落地与年度验收清单。
2. 同步更新 README 文件树与“各层级与各文件作用说明（逐项）”章节，确保新增文档职责说明与仓库结构一致。

## 本次更新内容（第 5 步：API 级测试补全、鉴权接入点与发布门禁说明）

1. **只读 API 测试补全**：`ParcelReadOnlyApiTests.cs` 新增 3 个测试用例，并扩展 `FakeParcelRepository`：
   - `GetAdjacentParcels_WithValidParams_ShouldReturnAdjacentItems`：邻近查询合法参数返回 200 + 结果列表（此前缺少成功路径覆盖）。
   - `GetAdjacentParcels_WithMissingScannedTime_ShouldReturnBadRequest`：未传 scannedTime 时返回 400，错误提示明确。
   - `GetAdjacentParcels_WithNegativeAfterCount_ShouldReturnBadRequest`：afterCount 为负时返回 400（补充 beforeCount 已有用例的对称覆盖）。
   - `FakeParcelRepository` 新增 `ShouldFailOnAdd` 属性，允许注入仓储写入失败，供管理端失败路径测试复用。
2. **管理端 API 测试补全**：`ParcelAdminApiTests.cs` 新增 1 个测试用例：
   - `CreateParcel_WhenRepositoryFails_ShouldReturn500`：当仓储 AddAsync 返回失败时，CommandService 抛 `InvalidOperationException`，Host 层捕获后返回 500，日志链路被覆盖（覆盖写接口失败路径）。
3. **鉴权接入点明确说明**：`ParcelAdminApiRouteExtensions.cs` 已有清晰注释标记两类接入点：
   - 普通写接口：`MapGroup` 上追加 `.RequireAuthorization("AdminPolicy")` 即可统一保护。
   - 危险治理接口（cleanup-expired）：追加 `.RequireAuthorization("DangerousActionPolicy")` 实施更严格管控。
   - 当前无鉴权框架，上述位置是唯一需修改的接入点，不伪装已有完整安全体系。
4. **发布门禁说明**：README 新增 Parcel API 最小发布门禁说明（见下文"Parcel API 发布门禁"节）。


## 本次更新内容（第 4 步：Parcel 管理端写接口）

1. **普通写接口**：在 `/api/admin/parcels` 路由组下新增 `POST`（新增包裹）、`PUT /{id}`（更新包裹状态，支持 MarkCompleted/MarkSortingException/UpdateRequestStatus）、`DELETE /{id}`（删除单个包裹）三个端点，统一经 Application 层应用服务访问仓储，不在 API 层直接操作实体状态。
2. **危险治理接口（单独分组）**：`POST /api/admin/parcels/cleanup-expired` 明确标记为治理型端点，不伪装为普通业务接口；返回 `ParcelCleanupExpiredResponse`（含决策 blocked/dry-run/execute、计划量、执行量、补偿边界）；隔离器由仓储内置，API 层不绕过。
3. **时间参数 UTC 防护**：新增 `LocalDateTimeParsing.cs` 共享工具（提取自 `ParcelReadOnlyApiRouteExtensions`），包含 `TryParseLocalDateTime`、`TryParseOptionalLocalDateTime`、`IsUtcKind` 方法，供两个路由扩展类共用，消除重复代码。
4. **Contracts 层扩充**：新增 `Enums/Parcels/ParcelUpdateOperation.cs`（操作类型枚举）及 `Models/Parcels/Admin/` 目录下四个合同模型（ParcelCreateRequest、ParcelUpdateRequest、ParcelCleanupExpiredRequest、ParcelCleanupExpiredResponse）。
5. **Application 层扩充**：新增四个写命令服务（CreateParcelCommandService、UpdateParcelStatusCommandService、DeleteParcelCommandService、CleanupExpiredParcelsCommandService），均为 `sealed class` + NLog + 步骤注释 + 异常日志。
6. **鉴权预留**：`ParcelAdminApiRouteExtensions.cs` 的 `MapGroup` 与治理端点处均有明确注释，指引如何追加 `.RequireAuthorization("AdminPolicy")`/`"DangerousActionPolicy"` 完成鉴权接入。
7. **测试补齐**：新增 `ParcelAdminApiTests.cs`（15 个测试用例），覆盖普通写接口成功路径、参数非法、cleanup-expired 三态；同时扩展 `FakeParcelRepository` 支持写操作与可配置三态 cleanup 行为，原有 92 个只读测试全部继续通过（合计 107 个测试）。


## 本次更新内容（第 3 步：暴露只读 Parcel API）

1. **Host 接入只读 Parcel 端点**：在 `Program.cs` 新增 `/api/parcels`、`/api/parcels/{id}`、`/api/parcels/adjacent` 三个只读查询端点，统一通过 Application 查询服务访问仓储，不在 API 层直接访问 `DbContext`，并保持不暴露领域实体 `Parcel`。
2. **参数绑定与最小必要校验**：列表端点支持分页与过滤参数绑定；详情端点对无效 Id 返回统一 400；邻近端点支持 `scannedTime + beforeCount + afterCount` 并校验条数非负与时间字符串本地语义（拒绝 UTC/offset）。
3. **统一错误输出风格与 Swagger 可见性**：三个端点统一输出 `ProblemDetails` 风格错误响应，并补充 `Produces/ProducesProblem` 元数据，确保 Swagger/OpenAPI 可见且响应契约明确。
4. **最小 API 级测试补齐**：新增 `ParcelReadOnlyApiTests.cs`，覆盖列表查询成功、详情查询成功、详情不存在返回 404、邻近参数异常返回 400 四个关键场景。


## 本次更新内容（Parcel API 合同层与应用层查询骨架）

1. **Contracts 补齐**：在 `Zeye.Sorting.Hub.Contracts/Models/Parcels/` 新增 `ParcelListRequest`、`ParcelListItemResponse`、`ParcelListResponse`、`ParcelDetailResponse`、`ParcelAdjacentRequest`、`ParcelAdjacentResponse`，建立对外查询合同，避免直接暴露领域实体 `Parcel`。
2. **Application 查询服务落地**：在 `Zeye.Sorting.Hub.Application/Services/Parcels/` 新增 `GetParcelByIdQueryService`、`GetParcelPagedQueryService`、`GetAdjacentParcelsQueryService`，统一在应用层调用 `IParcelRepository` 并向 Contracts 映射输出。
3. **映射收口**：新增 `ParcelContractMapper`，集中完成领域模型/读模型到合同模型的转换，避免 Host 层复制映射逻辑。
4. **最小参数校验与日志**：分页查询与邻近查询加入最小必要参数校验（页码/页大小、时间范围先后、枚举合法性、邻近数量非负与上限归一化），并在异常/非法参数路径使用 NLog 记录日志。
5. **测试补齐**：新增 `ParcelQueryServicesTests.cs`，覆盖应用层列表/详情/邻近查询骨架的映射与校验行为。


## 本次更新内容（危险批量删除治理收口）

1. **`RemoveExpiredAsync` 纳入隔离器边界**：`ParcelRepository.RemoveExpiredAsync` 复用 `ActionIsolationPolicy` + `ActionIsolationDecision`，将危险批量删除改为“开关 + dry-run + 审计”受控执行链路；默认配置为守卫开启、危险执行关闭、dry-run 开启，避免直接删库式清理。
2. **仓储契约最小增强**：`IParcelRepository.RemoveExpiredAsync` 返回值由 `RepositoryResult<int>` 收敛为 `RepositoryResult<DangerousBatchActionResult>`，显式表达 `blocked / dry-run / executed` 三态，并返回计划数量、执行数量与补偿边界说明。
3. **审计与边界声明补齐**：过期清理新增结构化审计日志字段（ActionName、CreatedBefore、PlannedCount、ExecutedCount、DryRun、BlockedByGuard、Reason、CompensationBoundary），并在结果与日志中明确“物理删除暂无自动回滚脚本，仅提供默认阻断 + dry-run + 审计 + 显式开关”的保守治理方案。
4. **配置与测试同步**：`appsettings.json` 新增 `Persistence:RepositoryDangerousActions:ParcelRemoveExpired:Isolator` 默认配置；`ParcelRepositoryTests` 新增三态回归（默认阻断、dry-run、显式放开后执行）以防治理回退。


## 本次更新内容（枚举注释与本地时间语义测试修复）

1. **枚举注释与 Description 补齐**：`Zeye.Sorting.Hub.Domain/Enums/ActionIsolationDecision.cs` 已为枚举类型与每个成员补齐中文 XML 注释，并为每个成员补充 `Description` 特性，保持原有成员语义与目录位置不变。
2. **UTC 直连语义移除（测试侧）**：`Zeye.Sorting.Hub.Host.Tests/AutoTuningProductionControlTests.cs` 将原先直接使用 `DateTimeKind.Utc` 的断言改为“非本地时间 Kind 输入触发异常”的等价治理断言，避免源码直接出现被禁 UTC 相关 API，同时保留“拒绝非本地时间语义输入”的测试目标。


## 本次更新内容（仓储 Result 类型统一收敛）

1. **统一仓储结果类型**：将 `RepositoryResult/RepositoryResult<T>` 迁移到 `Zeye.Sorting.Hub.Domain/Repositories/Models/Results/RepositoryResult.cs`，作为 Domain 与 Infrastructure 共用结果模型。
2. **移除重复结果模型**：删除 `Zeye.Sorting.Hub.Infrastructure/Repositories/RepositoryResult.cs`，仓储体系不再保留并行 Result 抽象。
3. **统一写操作契约**：`IParcelRepository` 的 `AddAsync/UpdateAsync/RemoveAsync/AddRangeAsync` 统一返回 `Task<RepositoryResult>`，`RemoveExpiredAsync` 统一返回 `Task<RepositoryResult<int>>`。
4. **去除无意义桥接与重复日志**：`ParcelRepository` 移除显式接口桥接抛异常逻辑，直接复用 `RepositoryBase` 返回语义；`RemoveExpiredAsync` 改为同一 Result 风格并保留批次删除 + 上限告警保护。
5. **测试与 DI 回归同步**：`ParcelRepositoryTests` 新增 `IParcelRepository` DI 解析用例，并将写操作断言统一到共享 `RepositoryResult` 契约。


## 本次更新内容（IParcelRepository 二次收敛）

1. **分页模型复用化**：将 `ParcelPageRequest/ParcelPageResult` 收敛为通用 `PageRequest/PageResult<T>`，以便其他查询场景复用，移除重复分页模型代码。
2. **读模型字段补齐**：`ParcelSummaryReadModel` 扩展为包含 `Parcel` 全部扁平化字段（含审计字段与业务标量字段），避免列表读模型信息缺失。
3. **去除重复查询契约**：删除 `GetDetailByIdAsync`，保留 `GetByIdAsync` 作为“按主键获取完整聚合详情”的唯一入口，避免接口语义重叠。
4. **强制时间段查询参数**：`GetByBagCode/GetByWorkstationName/GetByStatus/GetByChute` 增加必填 `scannedTimeStart/scannedTimeEnd` 参数，确保查询边界明确。
5. **时间跨度治理**：新增 `MaxTimeRangeAttribute` 并应用于 `ParcelQueryFilter`，默认限制扫码时间跨度不超过 3 个月（同时校验结束时间不早于开始时间）。
6. **Models 目录细分**：将 `Models` 细分为 `Filters/Paging/ReadModels/Validation` 子目录，职责清晰，便于后续扩展与复用。
7. **无用代码清理**：删除不再使用的 `ParcelPageRequest.cs` 与 `ParcelPageResult.cs`，同步更新引用与测试，避免冗余实现残留。


## 本次更新内容（IParcelRepository 第一阶段可落地仓储实现）

1. **仓储契约落地**：将 `IParcelRepository` 从注释占位改为可执行契约，仅覆盖第一阶段能力（基础读写、分页列表、按 Bag/工作台/状态/格口查询、邻近查询、过期清理与批量新增），不扩散到统计/报表能力。
2. **新增最小查询模型**：在 `Zeye.Sorting.Hub.Domain/Repositories/Models/` 新增 `ParcelQueryFilter`、`PageRequest`、`PageResult`、`ParcelSummaryReadModel`，实现“列表返回摘要读模型、详情返回聚合根”的职责分离。
3. **新增基础设施实现**：在 `Zeye.Sorting.Hub.Infrastructure/Repositories/ParcelRepository.cs` 新增第一阶段仓储实现，复用 `RepositoryBase` / `IDbContextFactory` 风格，提供列表查询、详情查询、邻近查询与基础写操作。
4. **DI 注册补齐且不破坏现有治理结构**：`PersistenceServiceCollectionExtensions` 新增 `IParcelRepository -> ParcelRepository` 注册，同时补充 `AddPooledDbContextFactory<SortingHubDbContext>` 并与既有 `AddDbContextPool` 复用同一配置方法，保持自动迁移/自动分表/自动调谐主结构不变。
5. **测试补齐**：新增 `ParcelRepositoryTests.cs`，覆盖第一阶段核心路径（分页过滤、详情与邻近查询、写操作与过期清理）。


## 本次更新内容（自动迁移 / 自动分表 / 自动调谐阶段收尾）

1. **自动迁移（当前阶段已完成）**：
   - 启动自动迁移链路保留（`DatabaseInitializerHostedService` 启动期迁移 + 一致性校验）。
   - 迁移一致性守卫与 `HasPendingModelChanges` 守卫已纳入启动流程。
   - 本次明确边界：当前阶段完成的是“自动迁移与一致性阻断”，**不包含**手工 DDL 自愈执行。
2. **自动分表（当前阶段已完成）**：
   - 主表与值对象分表规则继续通过声明式规则清单统一注册（`ParcelAggregateShardingRuleKind` 明确 `Date` / `Hash` 规则类别语义）。
   - Time / Volume / Hybrid 策略评估与治理审计保持闭环。
   - PerDay 手工预建窗口守卫、PerDay 物理分表存在性探测已完成。
   - 本次收尾修复两项关键问题：  
     a) 治理守卫从统一重试链路中拆出，配置/守卫/物理表明确缺失等确定性错误立即失败，不再被重试放大；  
     b) 批量物理探测接口补齐 `schemaName` 语义，HostedService 显式传参，SQL Server 不再写死 `dbo`。
   - 本次明确边界：当前阶段完成的是“策略决策 + 规则注册 + 治理守卫 + 物理存在性探测”，**不是**完整在线自动扩缩容平台。
3. **自动调谐（当前阶段已完成）**：
   - 慢 SQL 采集、闭环自治（监控/诊断/执行/验证/回滚）链路已具备。
   - 白名单、风险评分、高峰时段限制、危险动作隔离器已具备并默认生效。
   - 自动验证 / 自动回滚链路与审计语义已具备。
   - provider-aware probe 扩展点已具备，默认 probe 仍为 logging-only。
4. **README 同步收尾**：
   - 已补齐 `ParcelAggregateShardingRuleKind.cs` 在“仓库文件结构（当前）”与“各层级与各文件作用说明（逐项）”中的条目与职责说明。


## 本次更新内容（finer-granularity 配置与 README 一致性修复）

1. **修复 BucketCount 校验误判**：`ParcelShardingStrategyEvaluator` 调整为仅在 `ModeWhenPerDayStillHot=BucketedPerDay` 时强制要求 `BucketCount`，`PerHour/None + 无 BucketCount` 视为合法，同时保留“非 Bucketed 配置 BucketCount 报当前不会生效”的既有校验语义。
2. **修复默认 appsettings 非法示例组合**：`Zeye.Sorting.Hub.Host/appsettings.json` 保留 `ModeWhenPerDayStillHot=PerHour`，移除 `Bucket:BucketCount` 示例，避免启动期分表策略守卫误阻断。
3. **同步修正文档文件树**：补齐 README 顶部“仓库文件结构（当前）”中遗漏的 `数据库读写压力测试计划.md`、`Zeye.Sorting.Hub.Host/Enums/` 与 `MigrationFailureMode.cs`。


## 本次更新内容（单天仍超大下一层粒度扩展设计）

1. **布尔扩展信号升级为结构化规划模型**：`ParcelShardingStrategyEvaluator` 将 `RequiresFinerGranularityExtension` 升级为 `ParcelFinerGranularityExtensionPlan`，可表达是否继续细分、推荐模式（`PerHour`/`BucketedPerDay`/`None`）、推荐原因、是否要求预建守卫、生命周期（`PlanOnly`/`AlertOnly`/`FutureExecutable`）。
2. **配置模型收口并支持未来细粒度策略**：在 `Persistence:Sharding:Strategy:Volume:FinerGranularity` 下新增结构化配置（`ModeWhenPerDayStillHot`、`Lifecycle`、`RequirePrebuildGuard`、`Bucket:BucketCount`），避免继续平铺字段导致 evaluator 膨胀。
3. **治理守卫改为消费统一规划结果**：`DatabaseInitializerHostedService` 新增 `ShouldEnforcePerDayPrebuildGuard`，守卫判断从“只认 PerDay 的硬编码分支”收口为“统一决策对象 + 扩展规划结果”驱动，便于后续扩展到更多粒度策略时复用。
4. **收口重复语义实现（去影分身）**：`ParcelShardingStrategyEvaluator` 引入通用枚举解析路径（拒绝数字枚举、统一默认值与错误输出），消除 Mode/Granularity/Action 三处重复解析实现，减少后续扩展重复代码。
5. **边界保持不变（明确声明）**：
   - Bootstrap SQL 仍保持“失败告警后继续（degraded）”语义，本次未改。
   - Parcel 主表仍按 `CreatedTime` 分表，本次未改为 `ScannedTime`。
   - Volume/Hybrid 仍是“容量驱动的时间粒度治理”；finer-granularity 当前属于“设计与治理层能力”，不是完整在线自动执行平台。


## 本次更新内容（PR3 审查问题修复）

1. **守卫异常日志补全**：`DatabaseInitializerHostedService` 将分表治理守卫纳入 `try/catch` 统一启动流程，并对守卫异常追加 `LogCritical` 后再抛出，保证阻断启动场景具备结构化日志。
2. **守卫触发边界修正**：`TargetMod > CurrentMod` 校验改为仅在“手工预建 + 守卫开启”分支执行，避免自动建表场景被误阻断。
3. **枚举归档治理**：`MigrationFailureMode` 从 HostedService 内嵌枚举提取至 `Zeye.Sorting.Hub.Host/Enums/MigrationFailureMode.cs`，并补齐 `Description` 与注释，符合枚举目录化规范。
4. **测试同步修复**：`AutoTuningProductionControlTests` 改为引用独立枚举类型，保持原有策略解析断言不变。


## 本次更新内容（PR3：自动迁移 + 分表治理完善）

1. **迁移失败策略明确化**：`DatabaseInitializerHostedService` 新增 `FailureStrategy` 解析逻辑，支持 `Production/NonProduction` 分环境策略；默认行为明确为“生产 FailFast、非生产 Degraded”，并兼容历史 `FailStartupOnError`。
2. **分表预建程序化守卫**：在既有 `DatabaseInitializerHostedService` 启动流程中新增治理守卫，不再仅靠日志提醒；当关闭启动自动建表且启用守卫时，强制校验 Runbook 配置，生产环境额外要求结构化扩容阶段配置。
3. **治理配置结构化收敛**：`appsettings.json` 将 `HashSharding.ExpansionPlan` 从单文本方案升级为结构化字段（`CurrentMod`、`TargetMod`、`Stages[]`），减少“文本计划式字段”。
4. **分表规则注册去膨胀**：`PersistenceServiceCollectionExtensions` 将 Parcel 关联值对象分表规则改为单一声明式清单统一注册，覆盖守卫复用同一清单，避免注册与守卫双列表长期膨胀。
5. **最小回归测试补充**：`AutoTuningProductionControlTests` 新增迁移策略分环境解析、结构化扩容阶段解析、扩容摘要优先级等测试，确保改动行为可验证。


## 本次更新内容（PR2：自动调谐与自动索引建议修复）

1. **分表命中率口径修正**：`autotuning.sharding.hit_rate` 改为“可识别主表且不含跨表语义”才计入命中，避免过去“只要有 FROM 就命中”的误判。
2. **跨表查询识别补强**：`autotuning.sharding.cross_table_query_ratio` 在 JOIN 之外，新增子查询多 `FROM`、集合运算（`UNION/INTERSECT/EXCEPT`）与逗号连接识别。
3. **热点与容量样本源修正**：`hot_table_skew` 与容量预测采样改为基于全量慢 SQL 指标聚合，不再只依赖 `TuningCandidates` 子集，降低统计偏差。
4. **自动索引建议前置过滤**：在 AutoTuning 主链路内新增统一过滤规则，先检查模型静态索引覆盖/语义重复，再过滤低价值索引建议，避免冗余建议进入执行与日报输出。
5. **公共逻辑复用与去重**：`SlowQueryAutoTuningPipeline` 新增主表提取公共方法，`DatabaseAutoTuningHostedService` 直接复用，减少重复语义实现。
6. **最小回归测试补充**：新增子查询跨表识别、全量指标热点倾斜口径、自动索引过滤三类测试，确保逻辑修复可验证。
7. **文件树与职责同步**：本次未新增/删除文件；已更新 `README` 中相关文件职责描述与本次更新说明。


## 本次更新内容（重复代码治理 PR：去重/收口/命名统一）

1. **DatabaseDialects 去重收口**：将 `MySqlDialect.cs` 与 `SqlServerDialect.cs` 重复的索引列归一化和索引名构造逻辑收敛到 `DatabaseProviderExceptionHelper.cs`，方言类仅保留 SQL 拼装差异。
2. **删除无意义转发器**：移除 `PersistenceServiceCollectionExtensions.cs` 中仅参数透传的 `GetPositiveIntOrDefault` 中间层，调用方直接复用 `AutoTuningConfigurationHelper`。
3. **统一配置键拼装入口**：在 `AutoTuningConfigurationHelper.cs` 新增 `BuildAutoTuningKey` 与 `BuildAutonomousKey`，并替换 `DatabaseAutoTuningHostedService.cs`、`SlowQueryAutoTuningPipeline.cs` 内重复本地方法。
4. **统一本地时间归一化入口**：在 `AutoTuningConfigurationHelper.cs` 新增 `NormalizeToLocalTime`，并替换 `SlowQuerySample.cs`、`PersistenceServiceCollectionExtensions.cs` 的重复实现，继续保持“仅本地时间语义”约束。
5. **最小回归测试补充**：在 `AutoTuningProductionControlTests.cs` 增加键拼装、本地时间语义归一化与跨方言索引名哈希一致性的测试，保障去重重构不改变外部行为。
6. **文件树与职责同步**：本次未新增/删除文件，已同步更新本 README 相关条目的职责描述，确保文档与当前实现一致。


## 本次更新内容（数据库读写压力测试计划）

1. **新增 `数据库读写压力测试计划.md`**：针对本项目 MySQL + EFCore.Sharding 分表架构制定完整压测方案，包含：
   - **4 大测试场景**：纯写入（TPS 梯度加压）、纯读取（QPS 梯度加压）、混合读写（70% 写 + 30% 读）、30 分钟长时稳定性。
   - **与项目配置对齐的通过标准**：P99 ≤ 500 ms（对齐 `Persistence:AutoTuning:AlertP99Milliseconds = 500`）、错误率 < 1%（对齐 `Persistence:AutoTuning:AlertTimeoutRatePercent = 1`）、死锁 = 0。
   - **分表专项指标**：`autotuning.sharding.hit_rate ≥ 98%`、`hot_table_skew ≤ 2.0`（最热分片调用量 / 平均调用量，完全均匀时为 1.0）。
   - **完整执行步骤清单、监控 SQL、日志采集命令与结果记录模板**。
2. **更新 README 文件树章节**：在"各层级与各文件作用说明（逐项）"根目录节补充 `数据库读写压力测试计划.md` 与 `新数据库提供程序接入指南.md` 两个条目的职责说明（两文件均已存在，此前逐项说明缺失）。


## 本次更新内容（Parcel 状态拆分 + 异常类型细分）

1. **`ParcelStatus` 三态收敛**：`ParcelStatus` 仅保留 `Pending`（待操作）、`Completed`（已完成）、`SortingException`（分拣异常）三个状态，避免状态语义与异常原因混用。
2. **新增 `ParcelExceptionType` 枚举**：在 `Zeye.Sorting.Hub.Domain/Enums/ParcelExceptionType.cs` 增加 12 类分拣异常原因（接口响应异常、等待 DWS 数据超时、等待目标格口超时、无效目标格口、速度不匹配、锁格、叠包、灰度仪响应异常、位置检测异常、包裹丢失、机械故障、飘格），并补齐 `Description` 与注释。
3. **`Parcel` 聚合新增异常类型字段**：新增 `ExceptionType`（可空），仅在 `Status=SortingException` 时赋值；新增 `MarkSortingException` 领域方法统一维护状态与异常类型一致性。
4. **新增迁移与测试验证**：补充字段变更迁移，并在现有测试工程中增加状态枚举与异常状态流转的最小回归测试。


## 本次更新内容（SQL Server 迁移策略澄清 + 发布策略澄清 + EF 验收流水线）

1. **设计时工厂冲突修复**：保留单一 `IDesignTimeDbContextFactory<SortingHubDbContext>` 入口（`MySqlContextFactory`），支持通过 `-- --provider SqlServer` 切换到 SQL Server，避免双工厂导致 `dotnet ef` 枚举 `DbContext` 时报键冲突。
2. **SQL Server 迁移策略明确**：`EFCore数据库迁移指南.md` 明确当前采用“单迁移目录（同程序集）”策略，迁移统一放在 `Persistence/Migrations/`；通过 `-- --provider SqlServer` / `-- --provider MySql` 区分执行路径。
3. **发布策略配置化**：新增 `Persistence:Migration:FailStartupOnError`，支持“迁移失败降级运行（false）/ 失败即阻断启动（true）”双模式；发布前 EF 验收未通过仍阻断发布。
4. **新增 CI 门禁工作流**：新增 `.github/workflows/ef-migration-validation.yml`，在 MySQL + SQL Server 容器上真实执行 `dotnet ef migrations list` / `dotnet ef database update` / `dotnet ef migrations script`。
5. **索引与精度特征标记收敛**：`Parcel`、`BagInfo` 索引改为类级别 `[Index]` 声明，`decimal(18,3)` 统一改为 `[Precision(18,3)]`；同步生成 `UseAttributeBasedIndexesAndPrecision` 迁移以消除 PendingModelChanges。


## 本次更新内容（EF Core 9 升级完成 + HasPendingModelChanges() 守卫增强 + ORM 特征标记精简）

1. **EF Core 9 升级完成**：`Zeye.Sorting.Hub.Infrastructure.csproj` 中所有 EF Core 相关包升级至 9.0.14（EF Core 核心 + SqlServer + Design），Pomelo 升级至 9.0.0，EFCore.Sharding 升级至 9.0.10；同时显式覆盖 `Pomelo.EntityFrameworkCore.MySql.NetTopologySuite 9.0.0` 消除传递依赖版本警告。运行时框架仍为 `.NET 8`，无需升级框架。
2. **`HasPendingModelChanges()` 守卫集成**：`DatabaseInitializerHostedService.AssertMigrationConsistencyAsync()` 新增第三项检查：调用 EF Core 9 专属 API `db.Database.HasPendingModelChanges()`，检测代码实体模型是否存在尚未通过 `dotnet ef migrations add` 生成迁移的变更。一旦检测到差异，立即输出 Critical 日志，提示执行 `dotnet ef migrations add` 对齐模型。
3. **迁移快照 `ProductVersion` 更新**：`SortingHubDbContextModelSnapshot.cs` 与 `20260316184030_InitialCreate.Designer.cs` 的 `ProductVersion` 注解从 `8.0.23` 更新至 `9.0.14`，与安装的 EF Core 版本保持一致。
4. **`EFCore9升级计划.md` 更新**：状态从"计划"更新为"✅ 已完成"，附实际升级前后版本对照表，第三项守卫检查代码示例替换为实际集成代码，核查清单全部标注为已完成。
5. **`EFCore数据库迁移指南.md` 更新**：CodeFirst 守卫说明从"两项检查"更新为"三项检查（EF Core 9）"，新增 `HasPendingModelChanges()` 说明行，移除"EF Core 8 局限性"提示。
6. **`[Precision(18,3)]` 精度特征标记收敛**：所有 `decimal` 字段（`Parcel`、`VolumeInfo`、`WeightInfo`、`SorterCarrierInfo`、`ParcelPositionInfo` 共 24 个属性）统一改为 EF Core 原生 `[Precision(18,3)]` 标注；`ParcelEntityTypeConfiguration` 移除全部 `HasPrecision(18, 3)` 调用，配置文件进一步精简，迁移快照与 Designer.cs 同步更新。


## 本次更新内容（ORM 特征标记、appsettings 真实连接参数、EF Core 9 升级计划）

1. **appsettings.json 连接字符串改为真实参数格式**：`ConnectionStrings:MySql` / `ConnectionStrings:SqlServer` 使用本地开发默认账密（`root`/`Admin@1234`、`sa`/`Admin@1234`），与设计时工厂 Fallback 值一致；说明注释更新为"私有库，由专属技术人员维护"。
2. **数据模型添加 `[MaxLength]` ORM 特征标记**：在 `AuditableEntity`、`Parcel` 聚合根及所有值对象（`BagInfo`、`BarCodeInfo`、`WeightInfo`、`VolumeInfo`、`ParcelDeviceInfo`、`GrayDetectorInfo`、`StickingParcelInfo`、`ApiRequestInfo`、`CommandInfo`、`ImageInfo`、`VideoInfo`）的字符串属性上添加 `System.ComponentModel.DataAnnotations.MaxLength` 特征标记，无需在 Domain 层引入 EF Core 依赖。
3. **实体配置精简**：`ParcelEntityTypeConfiguration`（468 行 → 203 行）和 `BagInfoEntityTypeConfiguration` 移除所有冗余的 `HasColumnName()`（列名与属性名相同）、`IsRequired()`（非可空类型自动推断）、`HasMaxLength()`（已由 Domain 层特征标记承担）配置，仅保留 EF Core 专属配置（影子属性、关系、索引、表名）。`decimal` 精度现统一由 Domain 层 `[Precision(18,3)]` 特征标记声明（需 `Microsoft.EntityFrameworkCore.Abstractions` 依赖）。
4. **`EFCore9升级计划.md` 新增**：详细说明 EF Core 8 → 9 的升级计划，包含可行性结论（EF Core 9 支持 .NET 8，无需升级运行时框架）、受影响 NuGet 包列表、升级步骤、`HasPendingModelChanges()` 守卫增强代码示例、重要变更说明及回滚方案。



## 本次更新内容（EF Core CodeFirst 迁移完成 + 数据库日志落盘 + SqlServer 设计时支持）

1. **`MySqlContextFactory` 实现（`IDesignTimeDbContextFactory<SortingHubDbContext>`）**：将原本的空占位类补充为完整的设计时工厂实现，连接字符串从 `appsettings.json` 的 `ConnectionStrings:MySql` 读取（按目录树搜索），版本采用 AutoDetect → 兜底 8.0 两级策略。
2. **`SqlServerContextFactory` 新建**：新增 SQL Server 设计时工厂，连接字符串从 `appsettings.json` 的 `ConnectionStrings:SqlServer` 读取，确保两种数据库均可通过 `dotnet ef` CLI 生成迁移。
3. **初始迁移 `InitialCreate` 生成**：执行 `dotnet ef migrations add InitialCreate` 生成三个迁移文件（`20260316184030_InitialCreate.cs`、`20260316184030_InitialCreate.Designer.cs`、`SortingHubDbContextModelSnapshot.cs`），覆盖全部实体表（Parcels 主表及 14 个值对象属性表），建表与回滚逻辑完整。
4. **NLog 双路日志落盘**：使用 NLog 替换 Serilog，`logs/app-*.log` 记录全量日志，`logs/database-*.log` 记录数据库专属日志（EF Core 迁移、`DatabaseInitializerHostedService`、`DatabaseAutoTuningHostedService`、Persistence 层），按天归档，保留 30 天。低开销设计：异步队列 + keepFileOpen + optimizeBufferReuse。任何数据库异常均记录，不导致程序崩溃。
5. **CodeFirst 迁移一致性守卫（`AssertMigrationConsistencyAsync`）**：每次启动后检测未应用迁移与迁移历史差异，输出具体差异的迁移名称（在代码中但未应用 / 已应用但代码中不存在），不抛出异常，不阻止程序运行。
6. **`EFCore数据库迁移指南.md` 新增**：创建迁移使用说明文档，涵盖迁移架构总览、运行时自动迁移流程、CLI 命令速查、设计时工厂说明、分表与迁移的职责分离说明、数据库日志落盘说明及常见问题。
7. **`新数据库提供程序接入指南.md` 新增**：以 SQLite 为例，逐步说明接入第三种数据库提供器时需修改的文件（NuGet 包、方言实现、设计时工厂、DI 注册、appsettings、迁移生成），并附接入核查清单与常见注意事项。
8. **`README.md` 同步更新**：文件树与逐项说明反映上述所有新增文件。


## 本次更新内容（Copilot 限制规则全量整改）

1. **事件载荷规范化**：将 `DomainEvents/Parcels` 下事件参数迁移到 `Domain/Events/Parcels`，并统一改为 `readonly record struct`，满足“事件载荷必须定义在 `Events` 子目录且使用不可变值语义”的规则。
2. **枚举规范补齐**：补齐缺失的枚举类型注释；为 `AutoTuningUnavailableReason` 全部成员补齐 `Description` 与成员注释，避免语义漂移。
3. **异常日志补齐**：`RepositoryBase` 中所有 `OperationCanceledException` 分支补充 `LogWarning`，确保异常路径均可观测。
4. **注释覆盖补齐**：为生产代码中缺失的类字段与方法补齐 XML 注释，统一满足“字段必须有注释、方法必须有注释”的规则。


## 本次更新内容（代码质量全面审查——逻辑/性能/冗余/死代码/影分身）

1. **影分身代码消除（Issue #3）**：新增 `AutoTuningConfigurationHelper.cs`，将 `GetPositiveIntOrDefault`、`GetNonNegativeIntOrDefault`、`GetNonNegativeDecimalOrDefault`、`GetDecimalInRangeOrDefault`、`GetDecimalClampedOrDefault`、`GetBoolOrDefault`、`GetPositiveSecondsAsTimeSpanOrDefault`、`GetTimeOfDayOrDefault` 等八个配置辅助方法统一集中，彻底消除原先散落在 `SlowQueryAutoTuningPipeline.cs`、`DatabaseAutoTuningHostedService.cs`、`PersistenceServiceCollectionExtensions.cs` 三处的相同副本（影分身代码）。
2. **冗余同步机制消除（Issue #1 性能/冗余）**：`SlowQueryAutoTuningPipeline` 的 `_slowQueries` 字段原为 `ConcurrentQueue<SlowQuerySample>`，但所有读写路径均已包裹在 `lock (_queueSync)` 内，并发队列的无锁内部同步完全冗余。改为 `Queue<SlowQuerySample>` 并保留现有锁，消除双重同步开销，代码意图更清晰。同步移除因此不再需要的 `using System.Collections.Concurrent` 与 `using System.Globalization` 死引用。
3. **异步资源管理修复（Issue #2）**：`DatabaseAutoTuningHostedService.ExecuteSqlAsync` 使用 `using var scope = _scopeFactory.CreateScope()` 做同步释放，若 DI 容器中注册了 `IAsyncDisposable` 服务则无法正确异步销毁。改为 `await using var scope = _scopeFactory.CreateAsyncScope()`，确保异步资源完整释放。
4. **容量预测逻辑解耦（Issue #4 逻辑）**：`UpdateAutonomousSignals` 在 `_enableFullAutomation = false` 时直接返回，导致 `_enableCapacityPrediction = true` 的容量趋势预警被静默跳过，两个独立功能错误地相互绑定。重构后：表热度更新仅受 `_enableFullAutomation` 控制，容量快照与趋势告警独立受 `_enableCapacityPrediction` 控制，两者互不干扰。
5. **死代码清理（Issue #5）**：`RepositoryBase.cs` 中 `using System.Text` 从未被任何代码引用，已移除。


## 本次更新内容（代码质量审查与缺陷修复）

1. **RepositoryBase（逻辑 Bug 修复）**：`AddAsync`/`AddRangeAsync`/`UpdateAsync`/`RemoveAsync` 原本在创建 `DbContext` 后不调用 `SaveChangesAsync` 即释放，所有变更静默丢失。现已在每个变更操作结束前正确调用 `await db.SaveChangesAsync(cancellationToken)`，确保数据持久化。
2. **SlowQueryAutoTuningPipeline.Collect（死代码移除）**：`while` 循环结束后的 `if (_slowQueries.Count >= _maxQueueSize)` 分支永远不可达（循环保证退出时队列 Count < max），已移除该冗余分支。
3. **AutoTuningClosedLoopTracker（内存泄漏修复）**：`_stages` 列表在长时运行场景下无上限增长，每天可累积数万条记录。现增加 `MaxStageHistory = 1000` 上限，超出时从列表头部淘汰最旧记录，防止内存泄漏。
4. **Parcel（注释与错误消息对调修复）**：`TargetChuteId` 的 XML 注释为"实际落格 Id"（实为"目标格口"）、`ActualChuteId` 的注释为"理论落格 Id"（实为"实际落格"），两者语义相反；`Create()` 工厂方法中对应的校验错误消息同样互换。现已修正为正确语义："目标格口 Id（系统路由分配的理论落格位置）"与"实际落格 Id（包裹实际到达的格口位置）"。
5. **Parcel.Create（CreatedTime 未初始化修复）**：`AuditableEntity.CreatedTime` 为 `protected set` 属性，但 `Parcel.Create()` 工厂方法从未赋值，导致所有新建包裹的 `CreatedTime` 永远为 `DateTime.MinValue`。现已在工厂方法中补充 `CreatedTime = DateTime.Now` 赋值。
6. **SlowQueryAutoTuningPipeline.GetDecimalOrDefault（文化敏感与语义修复）**：原方法使用不带 `CultureInfo` 的 `decimal.TryParse`（文化敏感），在非英文系统可能解析失败；且 `parsed > 0` 条件错误拒绝 0 值，与同类方法（`GetNonNegativeDecimalOrDefault`）行为不一致。现已修正为 `NumberStyles.Number, CultureInfo.InvariantCulture` 且条件改为 `>= 0m`。


## 本次更新内容（剩余问题收口 PR）

1. **PerDay 守卫闭环**：在 `DatabaseInitializerHostedService` 既有治理守卫中补齐 “`CreateShardingTableOnStarting=false` + `EffectiveDateMode=PerDay`” 场景校验，确保按天治理策略具备完整前置校验，避免策略切换到按天后物理日表未预建。
2. **容量观测输入收敛**：`ParcelShardingStrategyEvaluator` 新增结构化 `Volume:Observation` 入口（`Source` / `EstimatedRowsPerShard` / `ObservedHotRatio`），并兼容 legacy 字段，实现“可配置输入 -> 统一观测对象 -> 阈值决策”的单入口收口，便于未来接入数据库统计/监控采集。
3. **Volume/Hybrid 语义澄清**：在策略决策日志、注释与配置说明中明确：`Volume/Hybrid` 是“容量阈值驱动的时间粒度治理”，当前**不是**独立的按数据量物理分表平台。
4. **扩展点预留**：策略决策中显式输出 `RequiresFinerGranularityExtension`，为“单天仍过大”后续扩展（如 `PerHour`/日内 bucket）保留入口；本次不引入新分表引擎。
5. **联动测试补强**：新增覆盖结构化 Observation 优先级、PerDay 预建日期格式校验、PerDay 策略在手工预建模式下的守卫阻断行为，确保治理边界可验证。
6. **有意保留项（本次明确不改）**：
   - Bootstrap SQL 仍保持“失败告警后继续”语义（不改为 fail-fast）。
   - Parcel 主表仍按 `CreatedTime` 分表（不改为 `ScannedTime`）。


## 本次更新内容（分表双策略可配置治理）

1. **新增分表策略配置模型与决策骨架**：在 `Persistence:Sharding:Strategy` 下支持 `Mode(Time/Volume/Hybrid)`、`Time.Granularity(PerMonth/PerDay)`、`Volume.MaxRowsPerShard`、`Volume.HotThresholdRatio`、`Volume.ActionOnThreshold(AlertOnly/SwitchToPerDay)`，并提供观测值字段用于阈值决策。
2. **统一策略评估入口（不新增平行框架）**：新增 `ParcelShardingStrategyEvaluator`，统一输出结构化校验结果与决策快照；`PersistenceServiceCollectionExtensions.ConfigureParcelAggregateSharding` 与 `DatabaseInitializerHostedService` 均复用该入口，避免影分身逻辑。
3. **注册入口收敛到现有分表链路**：继续使用 `ConfigureParcelAggregateSharding + ParcelAggregateShardingRules` 注册，不复制新框架；在保持 Parcel 主表分片字段为 `CreatedTime` 的前提下，根据策略决策动态选择 `PerMonth/PerDay`，并同步驱动日期型值对象分表粒度。
4. **治理守卫与审计补强**：`DatabaseInitializerHostedService` 新增分表策略校验失败日志与启动期守卫阻断（结构化错误清单），并在治理基线日志输出当前策略模式、阈值动作、阈值命中状态与最终粒度决策。
5. **边界保持不变（显式声明）**：
   - Bootstrap SQL 仍保持“失败告警后继续（degraded）”语义，本次未改为 fail-fast。
   - Parcel 主表仍按 `CreatedTime` 作为主分表依据，本次未改为 `ScannedTime`。


## 本次更新内容（分表治理与观测补强）

1. **分表自动创建默认关闭但制度化补强**：`Persistence:Sharding:CreateShardingTableOnStarting` 默认 `false`，并在启动期审计输出预建窗口（`Governance:PrebuildWindowHours`）与 Runbook（`Governance:Runbook`），降低“路由命中但物理表缺失”风险。
2. **哈希分片模数配置化并沉淀扩容计划**：`ParcelRelatedHashShardingMod` 从硬编码改为配置驱动（默认 16）；新增 `HashSharding:ExpansionTriggerRatio` 与 `HashSharding:ExpansionPlan`，显式记录 16→32 的触发阈值与切换步骤。
3. **分表观测指标显式沉淀**：`DatabaseAutoTuningHostedService` 新增三项指标输出：`autotuning.sharding.hit_rate`、`autotuning.sharding.cross_table_query_ratio`、`autotuning.sharding.hot_table_skew`，用于持续观察命中率、跨分表查询占比与热点倾斜。
4. **治理与容量预测绑定策略文档化**：通过 `appsettings.json` 的 Sharding/Governance/HashSharding 段将扩容触发阈值、预建窗口与 Runbook 配置显式化，便于纳入演练制度。
5. **新增自动审查守卫（EF + 分表联动）**：在现有 `HasPendingModelChanges()` 守卫基础上，新增 `AssertParcelAggregateShardingCoverage`（启动期 + 测试期双校验），当 ValueObjects 新增 `*Info` 类型但未配置分表规则时立即失败，阻断遗漏进入运行期。


## 本次更新内容（新增 Parcel 属性操作指南文档）

1. **新增 `Parcel属性新增操作指南.md`**：详细说明当 Parcel 聚合需要新增属性时，需要修改哪些文件及如何修改。文档涵盖三种情形：①在聚合根主表新增标量属性（修改 `Parcel.cs` + `ParcelEntityTypeConfiguration.cs` + 执行迁移）；②在现有值对象中新增属性（修改对应值对象文件 + `ParcelEntityTypeConfiguration.cs` + 执行迁移）；③新增全新值对象（新建值对象文件 + 修改 `Parcel.cs` + 修改 `ParcelEntityTypeConfiguration.cs` + 更新 `README.md` + 执行迁移）。附完整检查清单与当前 Parcel 主表字段一览。
